import MetaTrader5 as mt5

# ---------------------------------------------------------------------------
# Asset Class — จุดเดียวทั้งระบบที่จัดกลุ่ม symbol ตามประเภทตลาด
# ---------------------------------------------------------------------------
# 2026-08-18: เพิ่มตามคำสั่งผู้ใช้ หลังเจอว่า MIN_SL_DISTANCE_PCT (0.9% เดิม) tune มาจาก
# BTC/XAU เท่านั้น แล้วบล็อกเกือบทุกไม้ของ Forex เพราะ ATR/ราคา ต่ำกว่ากันโดยธรรมชาติของตลาด
# — ก่อนหน้านี้ทุกพารามิเตอร์ (vol_multiplier, wick_ratio_min, MIN_SL_DISTANCE_PCT,
# BAR_OFFSET_H) ต้องเดา/เช็คเองทีละ symbol ว่าควรได้ค่าไหน ทั้งที่คำตอบมักขึ้นกับ "ตลาดแบบไหน"
# เป็นหลัก ไม่ใช่ symbol เจาะจง — มี ASSET_CLASS กลางแล้ว พารามิเตอร์ที่มีเหตุผลเชิง class
# ชัดเจน (เช่น MIN_SL_DISTANCE_PCT ที่ผูกกับ ATR% ของตลาด) จะได้ default ตาม class ก่อน แล้ว
# ค่อย override เฉพาะ symbol ทับได้ถ้ามีข้อมูล backtest/tune จริงรายตัว (ไม่ใช่แทนที่ระบบ tune
# รายตัวที่มีอยู่แล้ว — symbol เดียวกันใน class เดียวกันยังต่างค่ากันได้ เช่น BTC/ETH wick
# ต่างกันเล็กน้อยทั้งที่เป็น CRYPTO เหมือนกัน)
#
# BAR_OFFSET_H (bars.py) "ไม่ใช้" ระบบนี้ — offset ผูกกับวิธีที่ TradingView ตัดแท่งของ feed
# นั้นๆ ซึ่งไม่สัมพันธ์กับ asset class เลย (วัดจริง: USDJPYm=1, EURUSDm=0, GBPUSDm=1 อยู่ใน
# class เดียวกันแต่ offset ไม่ตรงกัน) ต้องวัดเป็นรายตัวเสมอ
ASSET_CLASS = {
    "BTCUSDm": "CRYPTO",
    "ETHUSDm": "CRYPTO",
    "XRPUSDm": "CRYPTO",
    "XAUUSDm": "GOLD",
    "USDJPYm": "FOREX",
    "EURUSDm": "FOREX",
    "GBPUSDm": "FOREX",
    "US500m":  "INDEX",
}


def get_asset_class(symbol: str) -> str | None:
    """คืน asset class ของ symbol หรือ None ถ้ายังไม่ได้จัดกลุ่ม (symbol ใหม่ที่ยังไม่เพิ่ม
    ลง ASSET_CLASS ด้านบน) — ผู้เรียกต้อง fallback เป็น default กลางเองถ้าได้ None"""
    return ASSET_CLASS.get(symbol)


# ---------------------------------------------------------------------------
# Account & Risk Management
# ---------------------------------------------------------------------------
# 2026-08-01: เอา ACCOUNT_BALANCE (ค่า hardcode) ออก — ยอดเงินต้องดึงจาก
# mt5_connect.get_account_balance() สดทุกครั้งแทน ไม่งั้น lot size / daily loss guard
# จะคำนวณผิดถ้ายอดจริงในบัญชีไม่ตรงกับค่าคงที่นี้
RISK_PER_TRADE  = 0.02    # 2% ต่อไม้
MAX_DAILY_LOSS  = 0.06    # ขาดทุนสูงสุดต่อวัน 6%

# ---------------------------------------------------------------------------
# เพดานความเสี่ยงระดับพอร์ต (2026-09-12)
# ---------------------------------------------------------------------------
# ปัญหาที่ปิดตรงนี้: ก่อนหน้านี้ไม่มีด่านไหนเลยที่มองข้าม symbol — scan_symbol() เรียก
# mt5.positions_get(symbol=symbol) ทีละตัว, SLOT_PER_STRATEGY คุมแค่ในตัวเอง, และ
# MAX_DAILY_LOSS เช็คเฉพาะ "ไม้ที่ปิดไปแล้ววันนี้" (journal.check_daily_loss) ไม่ได้กัน
# การเปิดพร้อมกัน => ความเสี่ยงที่เปิดค้างอยู่พร้อมกันทั้งพอร์ตไม่เคยมีเพดาน
#
# วัดด้วย backtest_portfolio.py (รวม replay 7 symbol เป็นเส้นเดียว 2024-09 → 2026-09,
# 197 ไม้ +37.54R) — สองตัวเลขที่ต้องแยกกันให้ออก:
#   นับหัวไม้ดิบ      เปิดพร้อมกันสูงสุด 7 ไม้, 35% ของเวลาถือ >=3 ไม้
#   คิดความเสี่ยงจริง  **ไม่เคยเกิน 4.0R เลยตลอด 2 ปี**
# ต่างกันเพราะกฎ BE/ปิดบางส่วนปลดความเสี่ยงทิ้งเร็วกว่าที่ตาเห็น — ไม้ที่ขยับ SL ไป
# breakeven แล้วเหลือความเสี่ยง 0 แต่ยังนับเป็น "1 ไม้ที่เปิดอยู่" เพดานจึงต้องคิดจาก
# ความเสี่ยงที่ยังมีชีวิต ไม่ใช่จำนวนไม้ (เพดานนับหัวไม้ที่ 4 ไม้เสีย -2.65R ฟรีๆ)
#
# ต้นทุนของเพดานแต่ละระดับ (ΔR จาก baseline +37.54R):
#   2.5R  บล็อก 24 ไม้   -7.71R   << แพงเกินไป
#   3.0R  บล็อก  9 ไม้   +0.83R      (บวก แต่ในระดับ noise — ไม่ได้แปลว่าดีกว่า)
#   3.5R  บล็อก  3 ไม้   -2.15R
#   4.0R  บล็อก  0 ไม้    0.00R      (ไม่เคยยิงเลย = ไม่ได้คุมอะไร)
# เลือก 3.0R เพราะเป็นค่าต่ำสุดที่ยัง "ฟรี" และไปตรงกับ MAX_DAILY_LOSS=6% พอดี
# (3.0R x RISK_PER_TRADE 2% = 6%) — ขาดทุนสูงสุดที่เปิดค้างได้ = ขาดทุนสูงสุดต่อวัน
MAX_PORTFOLIO_RISK_R = 3.0

# เพดานต่อกลุ่ม symbol ที่วิ่งไปด้วยกัน — เพดานรวมข้างบนไม่รู้ว่า BTC+ETH 2 ไม้คือ
# เดิมพันเดียวขนาด 2 เท่า ไม่ใช่ 2 เดิมพันอิสระ วันที่ดอลลาร์วิ่งแรงวันเดียวก็เก็บทั้งกลุ่ม
# ต้นทุนที่วัดได้: 2.0R บล็อก 3 ไม้ -1.29R | 1.5R บล็อก 21 ไม้ -7.97R | 1.0R -15.66R
# เลือก 2.0R = ห้ามถือเต็มหน้าตักพร้อมกันเกิน 2 ไม้ในกลุ่มเดียว แต่ไม่ได้ห้ามถือคู่กัน
MAX_GROUP_RISK_R = 2.0

# symbol ที่ไม่อยู่ใน dict นี้ = อยู่กลุ่มของตัวเอง (เพดานกลุ่มจึงไม่มีผลกับมัน)
# จงใจจับเฉพาะคู่ที่เกาะกันแน่นจริงๆ ไม่เหมารวม XAU/US500 เข้ามาด้วย — ทั้งคู่ผูกดอลลาร์
# เหมือนกันก็จริง แต่ correlation ไม่คงที่พอจะ hardcode และการเหมารวมทำให้เพดานกลุ่ม
# กลายเป็นเพดานรวมอีกอันที่ต่ำกว่าเดิม
CORRELATION_GROUPS = {
    "BTCUSDm": "CRYPTO",
    "ETHUSDm": "CRYPTO",
    "EURUSDm": "EURGBP",
    "GBPUSDm": "EURGBP",
}

# ผลรวมของสองด่านนี้ตามที่ backtest_portfolio.py วัด: 187 ไม้ +37.41R (ΔR -0.13)
# = ตัดหางความเสี่ยงทิ้งโดยแทบไม่เสียผลตอบแทน
#
# ⚠️ ตัวเลข ΔR ข้างบนเป็น **ขอบบนของต้นทุน** ไม่ใช่ต้นทุนจริง — backtest_portfolio.py
#    เอาผล replay รายตัวมาต่อกัน ไม้ที่ถูกบล็อกจึงหายไปเฉยๆ ของจริงช่องจะว่างและมีสิทธิ์
#    รับไม้ถัดไปของ symbol นั้นแทน ซึ่ง replay รายตัวไม่เคยสร้างให้ดู

MIN_RR          = 2.0     # เกณฑ์ "R:R ดีจริง" ในสกอร์การ์ด (ให้คะแนนเท่านั้น ไม่บล็อกการเทรด)
                          # 2026-08-26: 1.5 -> 2.0 ตามคำสั่งผู้ใช้ — เดิมเท่ากับ MIN_RR_HARD_BLOCK
                          # เป๊ะ ทำให้ช่อง "R:R" ในสกอร์การ์ดเป็นแต้มฟรี: ไม้ที่ rr < 1.5 ถูก hard
                          # block raise ทิ้งไปก่อนถึงมือผู้เรียกอยู่แล้ว เงื่อนไข rr >= MIN_RR จึงเป็น
                          # จริง 100% เสมอ เหลือความหมายแค่ not used_fallback_tp = "Fibonacci หา TP
                          # ได้ไหม" (backtest BTC 730 วัน: active 97.5% = อัตราสำเร็จของ Fib เท่านั้น
                          # และมีไม้ถูก hard block ด้วย R:R ต่ำถึง 1,869 ครั้ง ซึ่งเกณฑ์นี้ไม่เคยมี
                          # โอกาสได้กรองเลย) ตั้ง 2.0 เพื่อให้มีช่วง 1.5 <= rr < 2.0 ที่ "ผ่าน hard
                          # block แต่ไม่ได้แต้ม" เกิดขึ้นจริง = ช่องนี้กลับมากรองอะไรได้
                          #
                          # 2026-08-26 (แก้ต่อในวันเดียวกัน): สูตร fallback TP ถูกแยกออกไปอิง
                          # MIN_RR_HARD_BLOCK แล้ว (ดู scoring.py) — ค่านี้จึงไม่กระทบ TP จริงของ
                          # ไม้ไหนอีกต่อไป มีผลแค่ "ได้แต้มหรือไม่ได้แต้ม" อย่างเดียว ปรับได้อิสระ
                          #
                          # หมายเหตุจาก backtest (BTC 730 วัน): ค่าในช่วง 1.5-2.5 ให้ผลเทรดเท่ากัน
                          # หมด (ไม้ที่ผ่าน MIN_SCORE: Total R -11 ถึง -19R แกว่งไปมาไม่มีทิศทาง)
                          # การขึ้นเป็น 2.0 จึงไม่ใช่การปรับปรุงผลตอบแทน แต่เป็นการทำให้สกอร์การ์ด
                          # ไม่โกหกตัวเอง — ที่ 3.0 เริ่มแย่ลงจริง (-25.9R) จึงไม่ควรเกิน 2.5
MIN_RR_HARD_BLOCK = 1.5   # (2026-08-26: ยังคง 1.5 — ตอนนี้ต่ำกว่า MIN_RR แล้วตามที่ควรเป็น
                          # ดู comment ที่ MIN_RR ด้านบน)
                          # 2026-08-02: ขึ้นจาก 1.0 -> 1.5 พร้อมกับเปลี่ยน TP เป็น 1.618 —
                          # ยกเกณฑ์ขั้นต่ำขึ้นชดเชย TP ที่ไกลขึ้น กันไม้ R:R ต่ำเกินไปเข้ามาปนเพิ่ม
                          # (แยกจาก MIN_RR แล้ว — เดิมใช้ค่าเดียวกันทำให้ hard block กับ
                          # คะแนนสกอร์การ์ดซ้ำซ้อนกัน เหมือนที่แก้ไปแล้วใน reversal.py)
#
# 🔴 2026-09-12: **ยกค่านี้ขึ้นแล้วแย่ลง — ทดสอบแล้ว อย่าลองซ้ำ**
# ที่มาของไอเดีย: แบ่งไม้ Scoring 177 ไม้เป็น 3 กองตาม R:R ตอนเข้า กองสูงสุด (>=2.57) ทำ
# avgR +0.33 เทียบกับ +0.06/+0.02 ของสองกองล่าง และไปทางเดียวกันทั้ง 7 symbol / 3 ปี
# ดูน่าเชื่อมาก แต่เป็นความสัมพันธ์ที่ **ไม่ใช่เหตุเป็นผล**
#
# replay จริง BTCUSDm 730 วัน --min-rr=2.5 (เทียบ baseline โค้ดวันเดียวกัน):
#   baseline     33 ไม้ +10.84R   Scoring 31 ไม้ +11.2R  avgR +0.36  โดน SL 39%
#   min-rr 2.5   29 ไม้  +4.57R   Scoring 27 ไม้  +4.9R  avgR +0.18  โดน SL 48%
#
# เหตุผลเชิงกลไก — ด่านนี้ **ไม่ได้ตัดไม้ทิ้ง มันเลื่อนไม้ออกไป**:
#   11 จาก 18 ไม้ที่ถูกบล็อก (61%) กลับเข้ามาใหม่ในการเคลื่อนไหวรอบเดียวกัน ช้าไป median
#   13 ชม. โดย R:R ขยับ 1.93 -> 3.31 และผลรวมของไม้คู่เหล่านั้น +0.43R -> +0.10R
#   ไม้ที่โผล่มาใหม่ทั้ง 14 ไม้ โดน SL 64% (baseline 39%)
# เพราะ R:R สูงขึ้นเองเมื่อราคาวิ่งเข้าหา SL (ระยะเสี่ยงหดลง) การ "รอให้ R:R ดีขึ้น" จึงเท่ากับ
# "รอให้ราคาย้อนมาใกล้ SL แล้วค่อยเข้า" = เข้าใกล้จุดตายกว่าเดิม โดนกินบ่อยกว่าเดิม
# ไม้ R:R สูงที่เกิดขึ้นเองตามธรรมชาติดีจริง แต่ไม้ R:R สูงที่ "ปั้นขึ้นมาด้วยการรอ" แย่
# (เป็นอาการเดียวกับ late entry ที่ MAX_RUNUP_24H_R กันอยู่ คนละทางเข้าเท่านั้น)
#
# ทดสอบซ้ำได้ด้วย: ./run_wine.sh backtest_replay.py BTCUSDm 730 --min-rr=2.5 --log-cuts
#
# 🔴 กลไกนี้กว้างกว่าเรื่อง R:R — **ใช้กับด่านคัดเข้าทุกตัวที่ "รอให้ตัวเลขดีขึ้น"**
# ตัวชี้วัดคุณภาพจุดเข้าดีขึ้นเองตอนราคาย่อ ทั้ง R:R (ระยะเสี่ยงหด) และ extension
# (|entry - pinned_swing| / ATR) ด่านที่รอจึงเปิดตอนย่อเสมอ
# แต่ extension ต่ำเกิดได้ 2 แบบที่แยกไม่ออกในข้อมูลตัดขวาง:
#   (1) ต่ำเพราะจับได้เร็ว เทรนด์ยังสด = ดี   (2) ต่ำเพราะย่อกลับ เทรนด์หมดแรง = แย่
# ด่านที่ทำงานด้วยการรอ ผลิตได้แต่แบบ (2) เพราะแบบ (1) ต้องเข้าตั้งแต่แรก
# วัดตรงๆ จากรอบ --min-rr=2.5: ไม้ที่ถูกเลื่อน 10/11 คู่มี extension ต่ำลง (3.5 -> 2.4 ATR)
# ซึ่งข้อมูลตัดขวางบอกว่าควรดีขึ้น แต่ผลจริง +0.24R -> -1.49R
#
# ⚠️ extension มีความสัมพันธ์ตัดขวางที่ดูแน่นมาก (7/7 symbol: ยืดน้อย +0.305R vs ยืดมาก
#    -0.089R, โดน SL 39% -> 51%) **อย่าเอาไปตั้งเป็นด่าน** มันเป็นตัวแปรเดียวกับ R:R
#    ในอีกมุม (corr -0.49) และพังด้วยกลไกเดียวกัน
# ถ้าจะเก็บ edge ของการเข้าเร็วจริง ต้องเข้าให้เร็วขึ้น = เรื่องความเร็วในการตรวจจับเทรนด์
# (get_trend_bias / trend_flip บน 1D) ไม่ใช่เรื่องตัวกรอง
MAX_RR_HARD_BLOCK = 15.0  # 2026-08-02: R:R ที่สูงเกินจริงมักมาจาก Fibonacci TP ยืดไกลเกินจริง

# 2026-09-05: เกณฑ์แยกสำหรับ Reversal — ตัวเลข 1.5 ถูกตั้งบนไม้บรรทัดที่ผิดมาตลอด (ด่าน R:R
# ของ Reversal ตรวจด้วย SL โครงสร้าง ทั้งที่เทรดจริงด้วย SL ที่ ATR ถอยออกไปอีก) พอแก้ให้ตรวจ
# ด้วย exec_sl ตัวจริง ไม้ Reversal หายไป 19 จาก 39 ไม้ และ Total R ตกจาก +3.64R เหลือ +1.62R
# ไม้ที่หายไปมี R:R จริงเฉลี่ย 1.17 (ต่ำสุด 0.90) แต่ **ชนะ 63%** — ที่ WR ระดับนั้นจุดคุ้มทุน
# ต้องการ R:R แค่ 0.6 เกณฑ์ 1.5 จึงเข้มเกินไปสำหรับโปรไฟล์ของไม้สวน (WR 59% ชนะบ่อยไม้เล็ก)
# ต่างจาก Scoring (WR 43% ต้องพึ่ง R:R สูง) จึงแยกค่าแทนที่จะลดตัวเดียวกันทั้งระบบ
#
# ทดลองลดเป็น 1.1 แล้ว (backtest_replay 8 symbol) — **ไม่เอา** คงไว้ที่ 1.5 เท่ากับ Scoring:
#   1.5 (ไม้บรรทัดถูก)  107 ไม้ WR 43.9% -0.03R   |   1.1  115 ไม้ WR 45.2% +0.46R
#   ได้ไม้ Reversal กลับมา 10 ไม้ (20 -> 30) แต่ Total R ของ Reversal กลับ *ลดลง* (+1.62 ->
#   +1.38R) = ไม้ที่ได้คืนมารวมกันติดลบ
# เหตุผลที่ลดเกณฑ์ไม่ช่วย: ไม้ Reversal ที่ขาดทุนหนักของ EURUSDm มี R:R จริง 1.31-1.49 ซึ่ง
# "ผ่าน" ทั้งเกณฑ์ 1.1/1.2/1.3 อยู่แล้ว ต้องยกกลับไป 1.5 ถึงจะกันได้ — และไม้ 19 ไม้ที่เคยดู
# เหมือนถูกตัดทิ้งอย่างน่าเสียดายนั้น ไม่ได้ดีเพราะ R:R ต่ำ แต่ดีเพราะบังเอิญเป็นไม้ของ
# GBPUSDm/XRPUSDm ที่ชนะ ส่วนไม้ R:R ต่ำของ EURUSDm ก็แพ้ตามปกติ => R:R ไม่ใช่ตัวแปรที่แยก
# ไม้ดีออกจากไม้เสียของ symbol เหล่านี้ (EURUSDm แพ้เพราะราคาไปไม่ถึง TP — ถึง TP แค่ 19%
# ต่ำสุดใน 8 symbol — ไม่ใช่เพราะ R:R ตอนวางแผนไม่ดี)
#
# ⚠️ ตัวเลขรวมตกจาก +3.20R เหลือ -0.03R หลังแก้ไม้บรรทัด — ยอมรับตามการตัดสินใจของผู้ใช้
# (2026-09-05) เพราะ +3.20R เดิมมาจากด่านที่ปล่อยไม้ R:R จริง 0.90-1.49 เข้ามาแล้วบังเอิญชนะ
# ในช่วง 2 ปีนี้ ไม่ใช่ edge ที่วัดได้ ถ้าเก็บไว้จะวัดอะไรต่อจากนี้ไม่ได้เลยเพราะฐานบิด
# คงเป็นค่าคงที่แยกไว้ (แม้ค่าจะเท่ากับ Scoring) เพราะสองกลยุทธ์คนละโปรไฟล์ ควรปรับแยกกันได้
MIN_RR_HARD_BLOCK_REVERSAL = 1.5
                          # เทียบกับ SL ที่อิงโครงสร้าง (ไม่ใช่ทิศทางที่ดีขึ้นจริง) — backtest
                          # 180 วัน XAU พบ 2 ไม้ RR=24.31/15.90 ทั้งคู่แพ้ ตัดออกทำให้ TotalR
                          # จาก -2.44R -> -0.44R
                          # หมายเหตุ: ค่านี้หามาจาก in-sample เดียวกับ MAX_TP_DISTANCE_PCT
                          # ด้านล่าง ยังไม่เคยทดสอบ out-of-sample — อาจ overfit เหมือนกัน
MIN_SL_DISTANCE_PCT = 0.9   # 2026-08-03: ระยะ entry->SL ต้องไม่แคบกว่ากี่ % ของราคา
                          # SL ของระบบอิงโครงสร้าง (วางถัดจาก swing point ล่าสุด) ถ้าจังหวะที่
                          # สัญญาณยิงราคาบังเอิญอยู่ติด swing point อยู่แล้ว ระยะ entry->SL จะแคบ
                          # มากโดยอัตโนมัติ = เข้าไม้ตอนยืนชิดกำแพง ขยับนิดเดียวก็ชน
                          # วัดจริงพบว่าไม้กลุ่มนี้ SL ห่างแค่ 0.38-1.30 เท่าของ ATR(4H) คือ
                          # แท่งเทียนธรรมดาแท่งเดียวก็กินระยะ SL หมดแล้ว
                          #
                          # ค่านี้ผ่าน backtest เต็มรูปแบบแล้ว (มี one-position slot dynamics ครบ
                          # — ตัวที่ทำให้ MAX_TP_DISTANCE_PCT เคยดูดีเกินจริงตอน naive sim) sweep
                          # threshold 0.0/0.6/0.7/0.8/0.9/1.0/1.2 ทั้ง 2 หน้าต่างเวลาที่ไม่ทับกัน:
                          #   floor=1.0 (ค่าเดิม): OOS -2.36->-1.36R ดีขึ้น, IN +20.05->+17.20R
                          #     แย่ลง -- เพราะไปบล็อกไม้ BTC risk=0.95% ที่ชนะใหญ่ RR=11.94 พอดี
                          #   floor=0.9: OOS -2.36->-1.36R (เท่า 1.0), IN +20.05->+21.05R ดีขึ้น
                          #     -- เส้นอยู่ต่ำกว่าไม้ BTC 0.95% พอดี เลยไม่โดนตัด แต่ยังจับไม้แย่
                          #     ตัวอื่นได้ครบ -- เป็นค่าเดียวใน sweep ที่ดีขึ้นทั้ง 2 หน้าต่างพร้อมกัน
                          #   floor=1.2: แย่ลงทั้ง 2 หน้าต่าง (ตัดไม้ดีเพิ่มโดยไม่ได้อะไรคืน)
                          # ผลข้างเคียงที่แย่อีกชั้นของ SL แคบ: lot ถูกคำนวณให้ระยะ SL x lot =
                          # RISK_PER_TRADE เสมอ SL ยิ่งแคบ lot ยิ่งบาน (ETH 2026-07-06 ต้องใช้ lot
                          # ใหญ่กว่าไม้ปกติ 4.3 เท่า) -> spread/slippage กินสัดส่วนความเสี่ยงมากขึ้น
                          # หมายเหตุ: threshold นี้ sensitive กับไม้เดี่ยวๆ ได้ง่าย (ตัวอย่าง BTC
                          # 0.95% ข้างต้น) และตัวเลข risk% เองก็มี noise เล็กน้อยจากการ fetch ข้อมูล
                          # ย้อนหลังคนละรอบ (ไม่ deterministic 100% ข้าม backtest run) ควรเฝ้าดูผล
                          # เทรดจริงต่อเนื่อง ไม่ใช่เชื่อเลข 0.9 นี้ว่าแม่นเป๊ะตลอดไป
                          #
                          # ⚠️ backtest ข้างบนทำกับ BTC เท่านั้น (ดู "ไม้ BTC risk=0.95%") ไม่เคย
                          # ทดสอบกับ Forex/Index เลย — ค่านี้เป็น % ของราคาตรงๆ ไม่ได้ปรับตาม
                          # ความผันผวนของแต่ละ asset class ปัญหาคือ ATR/ราคา ของ Forex ต่ำกว่า
                          # crypto/gold มาก (วัดจริง 2026-08-18: BTC 0.65%, XAU 0.74% vs EUR 0.13%,
                          # GBP 0.14%, USDJPY 0.16%, US500 0.24%) — SL ของ Forex ที่คำนวณจาก
                          # structure+ATR เดียวกันจะแคบกว่า BTC/XAU โดยธรรมชาติของมันเอง ไม่ใช่
                          # SL แคบผิดปกติแบบที่กฎนี้ตั้งใจจะกัน ถ้าใช้ 0.9% ตายตัวจะบล็อกเกือบทุกไม้
                          # ของ Forex (เจอจริง: USDJPYm 0.31%, EURUSDm 0.81%, GBPUSDm 0.28% ผ่านมา
                          # ไม่ได้สักไม้)

# ค่า override เฉพาะ symbol ของ MIN_SL_DISTANCE_PCT — symbol ที่ไม่มีในนี้ใช้ MIN_SL_DISTANCE_PCT
# ตัวบน (0.9%) เหมือนเดิม ไม่กระทบ BTC/XAU/ETH/XRP ที่ backtest ยืนยันแล้ว
#
# 2026-08-18: เพิ่มให้ Forex/Index ตามคำสั่งผู้ใช้หลังพบว่า 0.9% บล็อกเกือบทุกไม้ — คำนวณจาก
# สัดส่วน ATR/ราคา จริงของแต่ละ symbol เทียบกับ BTC (ซึ่งเป็นฐานของ backtest เดิม): ใช้ตัวคูณ
# 0.9/0.65 = 1.38 (0.9% ที่ tune ไว้ / ATR-to-price ของ BTC 0.65% ตอนวัด) คูณ ATR/ราคา ของ
# symbol นั้นๆ แล้วปัดเป็นเลขกลมๆ — เป็นการ "แปลงสัดส่วน" ไม่ใช่ backtest จริง ยังไม่มีข้อมูล
# เทรดจริงหรือ sweep ยืนยันว่าตัวเลขเหล่านี้เหมาะสม (ต่างจาก 0.9% ตัวบนที่ผ่าน backtest เต็ม
# รูปแบบมาแล้ว) ควรเฝ้าดูผลจริงและพร้อม sweep หาค่าที่แม่นกว่านี้เมื่อมีข้อมูลมากพอ:
#   USDJPYm 0.2%  (ATR/ราคา 0.16% x 1.38 = 0.22 ปัดลง)
#   US500m  0.3%  (0.24% x 1.38 = 0.33 ปัดลง)
#   EURUSDm 0.2%  (0.13% x 1.38 = 0.18 ปัดขึ้น)
#   GBPUSDm 0.2%  (0.14% x 1.38 = 0.19 ปัดขึ้น)
#
# 2026-09-08: **ทดสอบ "เพดาน" ความกว้าง SL แล้ว — ไม่เอา อย่าลองซ้ำ** (ตรงข้ามกับค่าพื้นด้านล่าง
# ที่กันไม่ให้ SL แคบเกิน) สมมติฐาน: SL/ATR เป็นตัวแปร ณ เวลาเข้าไม้ตัวเดียวที่แยก "ไม้ที่ไม่เคย
# ไปทางเราเลย" ออกจาก "ไม้ที่ชนะ" ได้ (ห่างกัน 0.62 SD ขณะที่ ADX 1H/4H = 0.06/0.11,
# ATR percentile 0.12, ระยะ EMA200 0.19, R:R แผน 0.28 = แยกไม่ออกทั้งหมด)
# กลไกฟังขึ้นมาก: SL ห่าง 19 ATR แปลว่าต้องให้ราคาวิ่ง 19 ATR ถึงได้ 1R ไม้พวกนี้ MFE 0.02-0.32R
# ทั้งที่ราคาขยับ 1-2 ATR ตามปกติ = แพ้เพราะเลขคณิต ไม่ใช่เพราะทิศผิด
# ผลจริง backtest_replay --max-sl-atr=10 ครบ 7 symbol 730 วัน:
#   ก่อน 181 ไม้ WR 49.7% +20.26R  ->  หลัง 179 ไม้ WR 49.7% +18.73R   (-1.53R, WR ไม่ขยับเลย)
#   ด่านคัดถูกตัวจริง: บล็อก 13 ไม้ที่ทำได้ -3.38R (WR 23.1%)
#   แต่ไม้ที่เข้ามาแทน 11 ไม้ทำได้ -4.91R (WR 18.2%) และ 10/11 คือ setup เดิมทิศเดิมที่เข้าช้าลง
#   (USDJPY 09-29 03:00 บล็อก -1.02R -> เข้าใหม่ 5 ชม.ถัดมา -1.03R)
# ⚠️ นี่คือครั้งที่ 3 ที่ "ด่านกรองไม้ทีละไม้" ล้มด้วยกลไกเดียวกัน (ตัวกรอง TP distance ที่จดไว้
# ใต้ MIN_SCORE, TP cap ที่จดไว้เหนือ TP_FIB_RATIO, และตัวนี้) — ช่องไม่เคยว่างเปล่า บล็อกไม้หนึ่ง
# ไม้ถัดไปจากกองเดียวกันก็เข้ามาแทน สิ่งที่ได้ผลจริงล้วนเป็นการเปลี่ยน "โครงสร้างของโอกาส"
# (แยกช่องตามกลยุทธ์ / ตัดทั้งประเภทอย่าง Reversal Short / ตัดทั้ง symbol อย่าง XRP)
# flag --max-sl-atr ยังอยู่ใน backtest_replay.py ไว้วัดซ้ำ แต่ไม่ได้ตั้งเป็น default
#
# 2026-08-18: ย้ายมาเป็น class-default + symbol-override (ดู ASSET_CLASS ด้านบนไฟล์) — CRYPTO/
# GOLD ไม่ต้องมี default แยกเพราะ MIN_SL_DISTANCE_PCT ตัวบน (0.9%) ผ่าน backtest เต็มรูปแบบ
# บน BTC/XAU อยู่แล้ว เป็นค่าที่ "ถูก" สำหรับ class นั้นโดยตรง ไม่ใช่แค่ fallback
MIN_SL_DISTANCE_PCT_BY_CLASS = {
    "FOREX": 0.2,   # ยังไม่มี backtest ยืนยัน — คำนวณจากสัดส่วน ATR/ราคา เทียบ BTC (ดูด้านบน)
    "INDEX": 0.3,   # เหมือนกัน
}

# symbol ที่ tune ค่าเฉพาะไว้แล้ว (แยกจาก class default เพื่อจำกัดผลของค่าที่วัดมาจาก symbol
# เดียวไม่ให้ลามไปตัวอื่นที่ยังไม่เคยทดสอบ)
#
# 2026-08-27: เคยตั้ง BTCUSDm = 2.0% แล้ว **ถอยกลับ** ในวันเดียวกัน — ค่านั้นหามาจาก
# backtest_criteria.py ซึ่งตอนนั้นยังไม่มีด่าน regime (scheduler.scan_symbol ข้ามการเปิดไม้
# เมื่อ regime อยู่ใน CHOPPY/เขตเทา/REVERSAL-WATCH = 68% ของรอบสแกน) พอวัดซ้ำด้วย
# backtest_replay.py ที่เดินตามด่านจริงครบ + exit_monitor ตัวจริง ผลกลับด้านทุกตัวชี้วัด:
#     เกณฑ์ 2.0%  25 ไม้  Win 44.0%  AvgR -0.22R  Total -5.6R  (ครึ่งแรก -6.1 / ครึ่งหลัง +0.5)
#     เกณฑ์ 0.9%  29 ไม้  Win 55.2%  AvgR +0.15R  Total +4.5R  (ครึ่งแรก -3.8 / ครึ่งหลัง +8.2)
# เกณฑ์ 2.0% ตัดไม้ที่ชนะทิ้ง (ไม้ที่จบด้วย TP หายจาก 6 เหลือ 3) — ความสัมพันธ์ "SL แคบ =
# win rate ต่ำ" ที่เคยวัดได้ ไม่รอดเมื่อวัดบนชุดไม้ที่ระบบเทรดจริง
MIN_SL_DISTANCE_PCT_BY_SYMBOL = {}


def get_min_sl_distance_pct(symbol: str) -> float:
    """ลำดับการหา: override เฉพาะ symbol -> default ตาม asset class -> MIN_SL_DISTANCE_PCT
    กลาง (0.9%, ผ่าน backtest เต็มรูปแบบบน BTC/XAU) — ใช้แทนการอ่าน
    MIN_SL_DISTANCE_PCT_BY_SYMBOL ตรงๆ ทุกจุดที่เคยทำ (scoring.py, reversal.py)"""
    if symbol in MIN_SL_DISTANCE_PCT_BY_SYMBOL:
        return MIN_SL_DISTANCE_PCT_BY_SYMBOL[symbol]
    cls = ASSET_CLASS.get(symbol)
    if cls in MIN_SL_DISTANCE_PCT_BY_CLASS:
        return MIN_SL_DISTANCE_PCT_BY_CLASS[cls]
    return MIN_SL_DISTANCE_PCT

MAX_TP_DISTANCE_PCT = 20.0  # 2026-08-03: TP ต้องไม่ห่างจาก entry เกินกี่ % ของราคา
                          # ทำงานคนละแกนกับ MAX_RR_HARD_BLOCK: ตัวนี้จับ "เป้าไกลเกินจนราคา
                          # ไปไม่ถึง", ตัวนั้นจับ "SL แคบผิดปกติจน RR พองขึ้น" (เช่น XAU
                          # 2026-06-26 TP ห่างแค่ 10.1% แต่ risk 0.42% -> RR=24.31)
                          #
                          # ⚠️ หลักฐานขัดแย้งกัน — เปิดใช้ตามการตัดสินใจของผู้ใช้:
                          #   in-sample  (180 วันล่าสุด): +11.96R -> +20.96R, win 26.7%->38.1%
                          #                               ไม้ที่ TP>20% แพ้ 9/9 (0%)
                          #   out-of-sample (180 วันก่อนหน้า): +1.94R -> -0.29R, win 32.0%->28.0%
                          #                               ไม้ที่ TP>20% ชนะ 2/5 (40%) — สูงกว่า
                          #                               ค่าเฉลี่ยรวมด้วยซ้ำ
                          # ตัวที่ทำให้ out-of-sample แย่ลงคือ ETH: ไม้ที่ชนะทั้ง 2 ไม้ในช่วงนั้น
                          # (2025-10-10 tpdist=24.1%, 2025-11-15 tpdist=31.1%) โดนตัวกรองนี้บล็อก
                          # และเพราะระบบเป็น one-position-at-a-time พอบล็อกไม้หนึ่ง "ช่อง" ที่ว่าง
                          # เปิดให้ไม้อื่นที่แพ้เข้ามาแทน (ETH 4 -> 7 ไม้ ทั้งที่เพิ่มตัวกรอง)
                          # => ควรเฝ้าดูผลเทรดจริงและพร้อมปรับกลับถ้าออกมาแย่ตาม out-of-sample
# 2026-09-07: **ทดสอบดึง TP เข้ามาแล้ว — แย่ลง อย่าลองซ้ำ** (backtest_replay --tp-cap-r=1.5
# ครบ 8 symbol 730 วัน) สมมติฐานที่ทดสอบ: TP ไกลเกินเอื้อม เพราะ R:R แผนเฉลี่ย 2.87 แต่ MFE
# เฉลี่ยแค่ 1.0-1.6R ต่อ symbol และไม้ถึง TP แค่ 25.6%
#   ก่อน  200 ไม้ WR 48.5% +17.83R   ->   หลัง 218 ไม้ WR 48.6% +10.97R   (-6.86R, แย่ลง 6/8)
# กลไกทำงานตามที่ออกแบบทุกอย่าง: ไม้ถึง TP 27.2% -> 42.1% และไม้ที่จบแบบ BE ลดจาก 29 เหลือ 11
# แต่ไม้ที่ถึง TP ได้แค่ +0.84R ไม่ใช่ +1.50R เพราะ exit monitor ปิดบางส่วนไปก่อนถึง TP อยู่แล้ว
# แม้ตรึงชุดไม้ให้เหมือนกันเป๊ะ (196 ไม้ที่เข้าตรงกัน) ก็ยังเสีย 6.11R และไม้ที่ปิดเร็วขึ้นทำให้
# ช่องว่างเร็วขึ้น มีไม้ใหม่เข้ามาอีก 22 ไม้ที่ทำได้ -1.57R (WR 40.9%)
# ที่คิดผิด: เอา MFE ของไม้ชนะ (+2.19R) กับไม้แพ้ (+0.49R) มาเฉลี่ยรวมกัน แล้วสรุปว่า TP ไกล
# เกินเอื้อม ทั้งที่ TP ไกลนั้น "เอื้อมถึงพอดี" สำหรับไม้ที่สำคัญ — ไม้ TP 46 ไม้ทำ +55.80R คือ
# แหล่งกำไรทั้งหมดของระบบ การดึง TP เข้ามาคือการแปลงไม้ใหญ่ให้เป็นไม้เล็ก
# => ปัญหาจริงอยู่ที่กฎปิดบางส่วน (ไม้ชนะเคยขึ้น +2.19R แต่เก็บได้ +0.83R = 38%) ไม่ใช่ที่ TP
TP_MAX_ATR = 18.0         # 2026-09-11: เพดานระยะ TP เป็นเท่าของ ATR ตอนเข้าไม้ (None = ปิด)
                          # ดึง TP เข้าด้วย min() ไม่ตั้งค่าตายตัว — ไม้ที่ fib ให้ TP ใกล้กว่านี้
                          # อยู่แล้วไม่ถูกยืดออก ใช้ atr_entry ตัวเดียวกับที่ compute_score ใช้
                          # คำนวณ exec_sl (sl_info["atr_entry"]) ไม่คำนวณ ATR ใหม่ซ้ำที่สอง
                          #
                          # คนละแกนกับ --tp-cap-r ที่ทดสอบแล้วแย่ลง -6.86R (ดูบล็อกเหนือขึ้นไป):
                          # ตัวนั้นวัดด้วย "ระยะเสี่ยงของไม้" (SL กว้าง = TP ยืดตาม จับไม้ผิดกลุ่ม)
                          # ตัวนี้วัดด้วย "ระยะที่ตลาดเดินได้จริง" จึงเจาะเฉพาะไม้ที่ตั้งเป้าไกลเกิน
                          # เอื้อม ที่มาของเลข 18 (backtest_replay 7 symbol 730 วัน 192 ไม้) —
                          # จัดกลุ่มไม้ตามระยะ TP แล้วมีหน้าผาชัดตรง 18 ATR พอดี:
                          #   6-9 ATR    34 ไม้  WR 67.6%  +11.50R  ถึง TP 44%
                          #   9-12       37      59.5%     +7.52    32%
                          #   12-18      53      54.7%    +18.35    40%
                          #   >18        61      42.6%     -2.56     8%   <- หน้าผา
                          # ผล: 192 ไม้ +32.92R -> 193 ไม้ +36.68R (+3.76R, ถึง TP 28.6->31.1%)
                          #
                          # ⚠️ อ่านก่อนจะไปจูนเลขนี้ต่อ — sweep 12/15/18 ครบ 7 symbol แล้ว
                          # (2026-09-11) **อย่าอ่านว่าตัวไหนชนะจาก R รวม** มันอยู่ในระดับ noise:
                          #   cap12  200 ไม้ +35.04R   cap15  194 ไม้ +37.05R   cap18  193 ไม้ +36.68R
                          # ทั้งสามระดับ "2 ไม้ใหญ่สุดมากกว่ากำไรสุทธิทั้งหมด" (18: 2 ไม้ +3.97R
                          # จากสุทธิ +3.08R) = ไม้ที่เหลือรวมกันติดลบทุกระดับ และไม้อันดับ 1 เป็น
                          # ไม้เดียวกันทั้งสามระดับ (USDJPY 2024-11-04 BE->TP) ตัดไม้นี้ไม้เดียว
                          # ออกอันดับสลับทันที เลือก 18 ไม่ใช่เพราะ R สูงสุด (15 สูงกว่า 0.37R)
                          # แต่เพราะมันแตะไม้แค่ 9 ไม้ (15 แตะ 20 ไม้) และเฉือนไม้ชนะไปแค่ -1.37R
                          # (15 เฉือน -3.58R) = ถ้าผลจริงไม่ตรง backtest ความเสียหายจำกัดกว่า
                          #
                          # กลไกที่วัดได้จริง — เพดานทำสองอย่างที่หักล้างกัน ที่ 18 ATR:
                          #   กู้ไม้ที่เคยกำไรแล้วถอยกลับมาจบ BE ได้  4 ไม้  +4.44R
                          #   เฉือนกำไรไม้ที่ชนะอยู่แล้ว              5 ไม้  -1.37R
                          # ฝั่งที่ทำเงินคือไม้ BE->TP = เอา TP มาชิงปิดก่อนกฎ BE จะคายกำไรทิ้ง
                          # **นี่คือการแก้ที่ปลายเหตุ** ต้นเหตุคือกฎปิดบางส่วนตามที่สรุปไว้เหนือ
                          # ขึ้นไป ("ไม้ชนะเคยขึ้น +2.19R แต่เก็บได้ +0.83R") ถ้าวันหนึ่งแก้ตรงนั้น
                          # ได้ ต้องกลับมาวัดเพดานนี้ใหม่ทั้งหมด ประโยชน์ของมันจะหายไปเป็นส่วนใหญ่
                          #
                          # ⚠️ ตัวเลข TotalR ทั้งหมดข้างบนเป็น "ผลเทียบกันในรอบเดียวกัน" เท่านั้น
                          # **รันวันหลังแล้วจะไม่ได้เลขเดิม** — replay ตั้ง start_time = แท่ง 1H
                          # สุดท้าย - days หน้าต่างจึงเลื่อนตามเวลาที่รัน พอไม้ที่ขอบหลุด/เข้ามา
                          # กฎ 1 ไม้ต่อช่องจะสลับไม้ถัดๆ ไปทั้งสาย วัดจริง (EURUSDm 2026-09-11
                          # เช้า vs เย็น ห่างกัน 12 ชม.): ไม้ 2024-09-11 04:00 (+1.58R) หลุดขอบ
                          # ไป ช่องเลยว่างให้ 2024-09-12 07:00 (-1.06R) เข้าแทน = **ยอดรวมของ
                          # symbol นั้นขยับ 2.64R จากไม้เดียว** ทั้งที่โค้ด/พารามิเตอร์เหมือนเดิมเป๊ะ
                          #   cap18  เช้า +1.01R  เย็น -1.62R    cap15  เช้า +1.51R  เย็น -1.13R
                          #   ส่วนต่าง cap15-cap18  เช้า +0.50R  เย็น +0.49R  <- อันนี้นิ่ง
                          # => เทียบตัวเลือกได้เฉพาะรอบที่รันหน้าต่างเดียวกัน และอ่าน "ส่วนต่าง"
                          # ไม่ใช่ "ยอดรวม" ถ้าจะ reproduce เลขข้างบนต้องรันทั้งชุดใหม่พร้อมกัน
                          #
                          # ⚠️ BTCUSDm (-0.68R) กับ US500m (-0.39R) ติดลบทุกระดับเพดาน (12/15/18)
                          # บนไม้ชุดเดิม = ไม้ใหญ่ของสองตัวนี้โดนเฉือนอย่างเป็นระบบ ไม่ใช่ noise
                          # ที่ยังไม่ทำเป็นค่าราย symbol เพราะ 192 ไม้แบ่ง 7 ตัวแล้วตัวละ 23-35 ไม้
                          # จูนแยกทีละตัวคือ curve fitting เต็มรูปแบบ (เหตุผลเดียวกับที่เก็บ ETHUSDm
                          # ไว้ทั้งที่ติดลบ ดูบล็อก SYMBOLS) — ถ้าจะแยก ต้องมีไม้จริงมากกว่านี้ก่อน
                          #
                          # EURUSDm/GBPUSDm ที่เป็นต้นเรื่องของการหาเพดานนี้ **ไม่ได้ถูกแก้**:
                          # EUR ไม่มีไม้เปลี่ยนผลแม้แต่ไม้เดียวที่ 18 ATR (ที่ 15 ได้ +0.49R จาก
                          # 2 ไม้) GBP ได้ +0.26R จากไม้เดียว ทั้งคู่ตั้งเป้าไกลจริง (21.1/17.9 ATR
                          # เทียบ 11.5-14.2 ของตัวอื่น เพราะ ATR/ราคาต่ำแค่ 0.11-0.12% แต่ fib ลาก
                          # จากโครงสร้าง swing ที่ไม่หดตาม) แต่ไม้ที่แพ้ส่วนใหญ่ไม่เคยวิ่งมาทางเรา
                          # ถึง 12 ATR ด้วยซ้ำ = แพ้ตั้งแต่ทิศ/จังหวะเข้า ไม่ใช่ตอนตั้งเป้า
TP_FIB_RATIO    = 1.618   # อัตราส่วน Fibonacci extension ที่ใช้วาง TP — จุดเดียวทั้งระบบ
                          # (scoring.py / reversal.py / backtest_reversal.py อ่านค่านี้ทั้งหมด)
                          #
                          # 2026-08-02: เปลี่ยนจาก 0.786 -> 1.618 — backtest 180 วันพบ BTC ดีขึ้น
                          # ชัดเจน (TotalR/win rate สูงขึ้น) ส่วน XAU แย่ลงเพราะ TP ยืดไกลเกินจริง
                          # บางเคส — ชดเชยด้วย MAX_RR_HARD_BLOCK=15 (บล็อกเคส TP ยืดสุดโต่งไปแล้ว)
                          # และยก MIN_RR_HARD_BLOCK ขึ้นเป็น 1.5
                          #
                          # 2026-08-13: ย้ายออกมาเป็น config — เดิม hardcode เป็น string key
                          # `fib_info["levels"]["1.618"]` ซ้ำ 3 ที่ แล้ว **หลุด sync กันจริง**:
                          # backtest_reversal.py ยังค้างที่ 0.786 พร้อม comment ว่า "ตรงกับ
                          # reversal.py ปัจจุบัน" ทั้งที่ reversal.py เปลี่ยนไป 1.618 ตั้งแต่
                          # 2026-08-02 => backtest วัดคนละสูตรกับระบบจริงมา 11 วันโดยไม่มีใครรู้
                          # ตอนนี้ find_tp_from_fibonacci() คืน key "tp" ที่คำนวณจากค่านี้ให้เลย
                          # ไม่ต้องไปหยิบจาก levels ด้วย string key อีก (เปลี่ยนค่าที่นี่ที่เดียว
                          # แล้วมีผลทั้ง live และ backtest พร้อมกัน)
# ---------------------------------------------------------------------------
# ด่านกันเข้าไม้ "ตอนปลายทาง" — ราคาวิ่งไปทางที่จะเข้ามาแล้วเกินกี่ R ใน 24 แท่ง 1H
# ---------------------------------------------------------------------------
# 2026-09-05: วัดไม้ Scoring 90 ไม้ครบ 8 symbol ใน SYMBOLS (backtest_replay 730 วัน หลังแก้บั๊ก
# lookahead + ปิด structure break) พบว่า "ราคาวิ่งมาแล้วเท่าไหร่ก่อนเข้า" แยกไม้ดี/ไม้เสียได้ชัด
# กว่าคะแนนสกอร์การ์ดเสียอีก:
#   ย่อลงมาหาเรา (<0)   10 ไม้  WR 60.0%  +5.53R      วิ่งไปแล้ว 0-0.3R   32 ไม้  WR 40.6%  -4.18R
#   วิ่งไปแล้ว 0.3-0.6R 44 ไม้  WR 31.8%  -4.77R      วิ่งไปแล้ว >0.6R     4 ไม้  WR 50.0%  +0.20R
# กลไกสอดคล้องกันทั้งแถว: ยิ่งวิ่งไปก่อนแล้ว SL ยิ่งห่าง (1.89% -> 3.07%) R:R แผนยิ่งแคบ
# (4.28 -> 2.86) MFE ยิ่งต่ำ (+2.46 -> +1.11) = เข้าตอนที่ move กินไปเยอะแล้ว
#
# เลือกค่า 0.5 จาก sweep 0.2/0.3/0.4/0.5 (ทุกค่าดีกว่าไม่กรองทั้งหมด = ไม่ใช่ noise):
#   ไม่กรอง 130 ไม้ WR 45.4% +1.44R   |   0.2  114 ไม้ WR 50.0% +4.95R   |   0.5  123 ไม้ WR 48.0% +3.20R
# 0.2 ให้ Total R สูงกว่าแต่ **ไม่เลือก** เพราะมันชนะจาก 2 symbol สุดขั้ว (BTC +4.15 / XAU -3.73)
# ไม่ใช่ดีสม่ำเสมอ — ที่ 0.2 มันตัดไม้ XAU ทิ้ง 4/15 ไม้แล้วพลิก XAU จาก +1.16R เป็น -2.57R
# ส่วน 0.5 ไม่แตะ XAU เลยสักไม้ และกรองแม่นกว่ามาก:
#   0.2 กรองออก 56 ไม้ -2.76R (ชนะ 36%)  |  0.5 กรองออก 20 ไม้ -3.18R (ชนะ 30%)
# ทั้งคู่ดีขึ้น 5/8 symbol เท่ากัน
# ⚠️ edge เกือบทั้งหมดมาจาก BTC (+1.30R) และ ETH (+0.60R) ส่วน EURUSDm (-0.94R) กับ GBPUSDm
# (-0.39R) แย่ลง — ไม่ได้ได้ผลทุกตลาด ควรเฝ้าดูผลจริงและพร้อมแยกค่าตาม symbol/asset class
# ใช้กับทาง Scoring เท่านั้น (Reversal เข้าสวนเทรนด์โดยดีไซน์ ตัวเลขนี้มีความหมายคนละแบบ)
# None = ปิดด่านนี้
MAX_RUNUP_24H_R = 0.5

# 2026-09-07: ถือได้ 1 ไม้ต่อ *กลยุทธ์* ต่อ symbol แทน 1 ไม้ต่อ symbol — แก้ปัญหาที่จดไว้ใน
# ⚠️ ข้อ 1 ใต้ MIN_SCORE (Scoring ครองช่องจน Reversal ไม่เหลือรอบเข้า) regime แยก
# TREND / REVERSAL-READY ชัดอยู่แล้ว สองกลยุทธ์จึงไม่มีทางยิงพร้อมกันในรอบเดียว
# backtest_replay --slot-per-strategy ครบ 8 symbol 730 วัน:
#   1 ช่อง  194 ไม้ WR 45.4%  +4.44R   Scoring 180 ไม้ +5.81R  |  Reversal 14 ไม้ -1.37R
#   2 ช่อง  218 ไม้ WR 45.9% +10.12R   Scoring 181 ไม้ +9.04R  |  Reversal 37 ไม้ +1.08R
# ดีขึ้น 5/8 symbol (XRP +2.99 / XAU +1.43 / ETH +0.95 / USDJPY +0.93 / EUR +0.45 ;
# แย่ลง US500 -0.63 / GBP -0.41 / BTC -0.05 = เล็กทั้งหมด)
# ไม้ Reversal ที่ได้กลับมา 23 ไม้ +2.45R และ Scoring ดีขึ้น +3.23R เองด้วยทั้งที่จำนวนไม้
# เท่าเดิม (181 vs 180) เพราะลำดับการเข้าไม้เปลี่ยนไปทั้งเส้น
# ⚠️ **ตัวเลขข้างบนคือ "ถ้ายอมเสี่ยง 2 เท่า"** — ถือ 2 ไม้พร้อมกันต่อ symbol โดย RISK_PER_TRADE
#    ยังเป็น 2% ทั้งคู่ = ความเสี่ยงสูงสุดต่อ symbol เป็น 4% ของพอร์ต backtest บวก R ตรงๆ ไม่ได้
#    ปรับ sizing ให้ ถ้าลด RISK_PER_TRADE เป็น 1% เพื่อคุมความเสี่ยงรวมให้เท่าเดิม R จะเหลือ
#    ราวครึ่งหนึ่ง (ยังดีกว่าเดิม แต่ไม่ใช่ของฟรี) — ยังไม่ได้ตัดสินใจเรื่องนี้ ค่าปัจจุบันคือ
#    "เสี่ยง 2 เท่า" ตามที่ backtest วัด
#
# 2026-09-12: คำเตือนข้างบนถูกปิดด้วย MAX_PORTFOLIO_RISK_R แล้ว (ไม่ใช่ด้วยการลด
# RISK_PER_TRADE) — เพดานคุมความเสี่ยงที่เปิดค้างรวมทั้งพอร์ตไว้ที่ 3.0R จึงเป็นไปไม่ได้
# ที่จะถือ 2 ไม้เต็มหน้าตักใน symbol เดียวพร้อมกับไม้เต็มอีก 2 ไม้ที่อื่น ตัวเลข R ของ
# 2 ช่องข้างบนยังอ่านได้เหมือนเดิม เพราะเพดานที่ 3.0R แทบไม่บล็อกอะไร (ΔR -0.13)
SLOT_PER_STRATEGY = True

# 2026-09-07: Reversal ฝั่ง Short เข้าได้เฉพาะตอนเทรนด์ 1D (get_trend_bias) เป็น Short ด้วย
# ที่มา: บน 8 symbol ไม้ Reversal Short 18 ไม้ WR 27.8% -6.08R เทียบกับ Long 19 ไม้ 57.9%
# +7.16R — และปัญหาอยู่ที่ "จุดเข้า" ไม่ใช่กฎ exit: ทดสอบ first-touch ด้วย SL/TP แผนเดิมโดย
# ไม่มีกฎ exit เลย ฝั่ง Short แตะ SL ก่อน 15/16 = WR ดิบ 6.2% ที่จุดคุ้มทุน 29.4% (สุ่มยังได้
# ดีกว่านี้) ส่วน Long ได้ 10/19 = 52.6% ที่จุดคุ้มทุน 26.2%
# สาเหตุที่วัดได้: RSI 4H อยู่เหนือ 70 บ่อยกว่าต่ำกว่า 30 ราว 1.7 เท่าทุก symbol (US500 11.1%
# vs 3.9%, XAU 10.6% vs 3.6%, BTC 6.8% vs 4.1%) — "overbought" คือสภาพปกติของตลาดที่ไต่ขึ้น
# ไม่ใช่สัญญาณกลับตัว bearish divergence ที่ key level จึงยิงใส่ความแข็งแรงธรรมดา
# (ไม่ใช่บั๊กลำดับใน check_divergence ที่ตรวจ bearish ก่อน return — วัดแล้วสองฝั่งเข้าเงื่อนไข
#  พร้อมกันแค่ 5/7472 รอบ = bullish โดนบัง 1%)
# ผลจริง backtest_replay --slot-per-strategy --rev-short-1d ครบ 8 symbol 730 วัน:
#   ก่อน  218 ไม้ WR 45.9% +10.12R      หลัง  200 ไม้ WR 48.5% +17.84R   (+7.72R)
#   ดีขึ้น 7/8 symbol (US500 +3.93 / ETH +1.03 / EUR +1.02 / USDJPY +0.79 / XAU +0.41 /
#   GBP +0.41 / BTC +0.14) เหลือ XRP -0.02 = เสมอ
# ด่านนี้ทำงานถูกเหตุ ไม่ใช่บังเอิญ: จาก 18 ไม้ Short เทรนด์ 1D เป็น Long 17 ไม้ / Short 1 ไม้ /
# None 0 ไม้ — ไม่มีไม้ไหนโดนบล็อกเพราะหาเทรนด์ไม่เจอ และไม้เดียวที่ผ่านด่าน (XRP 2026-07-06)
# คือไม้ Short ที่กำไรจริงไม้เดียวในชุด (+0.43R)
# แยกผลตามเทรนด์ 1D: Short/1D-Long 17 ไม้ -6.51R · Short/1D-Short 1 ไม้ +0.43R ·
#                    Long/1D-Short 14 ไม้ +4.92R · Long/1D-Long 5 ไม้ +2.25R
# -> "สวนเทรนด์" ไม่ใช่ปัญหา (Long สวนเทรนด์ 1D ขาลงทำ +4.92R) ปัญหาคือความไม่สมมาตรของตลาด
# ใช้กับฝั่ง Short เท่านั้น — ฝั่ง Long ไม่แตะเพราะทำงานได้ดีอยู่แล้ว
REVERSAL_SHORT_NEEDS_1D_TREND = True

# 2026-09-06: 5.0 -> 0.0 = **ปิดด่านสกอร์การ์ดของ Scoring ทั้งชุด** (เกณฑ์ยังถูกคำนวณและบันทึก
# ลงไม้ทุกไม้เหมือนเดิม แค่ไม่ได้ใช้กรอง) ตามการตัดสินใจของผู้ใช้ หลังทดสอบด้วย
# backtest_replay --min-score=0 ครบ 8 symbol 730 วัน:
#
#   ปัจจุบัน (5)   Scoring  87 ไม้ WR 42.5% -1.65R  |  Reversal 20 ไม้ +1.62R  |  รวม -0.03R
#   ไม่มีด่าน (0)  Scoring 180 ไม้ WR 45.6% +5.81R  |  Reversal 14 ไม้ -1.37R  |  รวม +4.44R
#
# เหตุผล: วัดบนไม้ 180 ไม้ (sample ใหญ่สุดที่เคยมี) แล้ว **ไม่มีเกณฑ์ไหนพิสูจน์ได้ว่ามีประโยชน์**
#   คะแนนไม่สัมพันธ์กับผลเลย: 1/6 ได้ WR 53.6% +8.99R แต่ 6/6 ได้ WR 44.8% -1.48R
#   ΔWR รายเกณฑ์: OBV 1D -12.1 / MACD 4H -9.5 (กลับด้าน) · DI 1H -3.3 ·
#                 RSI 4H Rebound +0.1 · Trend 1H +0.2 · R:R +0.6 (สามตัวหลัง = แยกจากศูนย์ไม่ออก)
#   ใช้ทีละเกณฑ์เป็นด่านเดี่ยว: ตัวที่ดีสุดทำให้ผลต่อไม้ดีขึ้นแค่ +0.03R และทุกคู่ 2 เกณฑ์
#   ให้ Total R ต่ำกว่า "ไม่กรองเลย" ทั้งหมด
#
# ⚠️ ราคาที่ต้องจ่าย 2 อย่าง (รู้ตัวแล้วและยอมรับ):
#   1. Reversal เสีย 3.0R ทั้งที่ไม่ได้แตะเกณฑ์มันเลย — Scoring เข้าไม้เพิ่มเท่าตัวแล้วครองช่อง
#      รอบ REVERSAL-READY ที่จบด้วย "ถือไม้อื่นอยู่" เพิ่มจาก 37% เป็น 58% ไม้ที่หายคือไม้ดี
#      (BTC +1.65->+0.88R, ETH +1.33->+0.37R, XAU 1->0 ไม้) — ทางแก้ที่ยังไม่ได้ทดสอบคือให้ถือ
#      ได้ 1 ไม้ต่อ *กลยุทธ์* ต่อ symbol (regime แยก TREND/REVERSAL-READY ชัดอยู่แล้ว) แต่ต้อง
#      คิดเรื่อง sizing ใหม่เพราะกลายเป็นถือ 2 ไม้พร้อมกัน
#   2. กำไรมาจาก "เทรดเยอะขึ้น 81%" ไม่ใช่คุณภาพต่อไม้ (Avg R -0.00 -> +0.02) = แลกคุณภาพเป็น
#      ปริมาณ ซึ่ง backtest จะดูดีเกินจริงเสมอเพราะไม่มี news guard / slippage / lot rounding
#      และยิ่งเทรดถี่ยิ่งโดนหนัก — ควรเฝ้าดูผลเทรดจริงใกล้ชิดหลังเปลี่ยนค่านี้
#
MIN_SCORE       = 0.0     # 0 = ไม่ใช้สกอร์การ์ดกรอง (เดิม 5.0 จาก TOTAL_WEIGHT=6)
                          # 2026-08-30: ค่าเท่าเดิม แต่ TOTAL_WEIGHT ลดเป็น 6 พร้อมกับตัด
                          # OBV 4H ออก -> ยอมให้ตกได้ 1 ข้อ (เดิม 2) ดูเหตุผล+ข้อจำกัดของ
                          # หลักฐานที่ comment เหนือ WEIGHT_OBV_1D ด้านล่าง
                          # 2026-08-27: 7/9 -> 5/7 พร้อมกับตัด Trend 1D (น้ำหนัก 2) ออก —
                          # ดูเหตุผลเต็มที่ WEIGHT_OBV_1D ด้านล่าง จำนวนเกณฑ์ที่ต้องผ่านจริง
                          # เท่าเดิม เพราะ 2 แต้มที่หายไปเป็นแต้มที่ทุกไม้ได้อยู่แล้ว
                          # 2026-08-09: กลับไป 7.0 (ตัดกลับจาก 6.0) — ไล่ backtest_score.py ย้อน
                          # ไม้ BTC Long ที่เข้าจริง 3 ส.ค. 01:40 พบว่าตอนเข้าคะแนนแค่ 6.0 พอดีเป๊ะ
                          # (Trend 1D/4H ยัง FAIL ทั้งคู่ ผ่านได้เพราะ Trend 1H/DI 1H ผันผวนช่วยดัน
                          # ชั่วคราว) ราคาย่อต่อเกือบชน SL ก่อนจะกลับมากำไร — เทียบกับ 21:00 วันเดียว
                          # กัน (คะแนนก็ 6.0 เฉียดเหมือนกัน) vs 23:00 (7.0, Trend 4H ยืนยันด้วย มี
                          # margin) เห็นชัดว่า 6.0 คือ "ผ่านแบบไม่มี margin" ปล่อยให้ timeframe สั้น
                          # (1H) ที่ผันผวนง่ายชดเชย Trend 1D (weight หนักสุด 2.0) ที่ยัง FAIL ได้
                          #
                          # 2026-07-27 (ประวัติเดิม): เคยลดจาก 7.0 มา 6.0 พร้อมตัด VSA ออก (เต็ม
                          # 10->9) จาก backtest ตอนนั้น (BTC +23.00R/14 ไม้, XAU +31.36R/17 ไม้) —
                          # sample เล็ก (14-17 ไม้) ยังไม่ conclusive ควรเฝ้าดูผลเทรดจริงรอบใหม่หลัง
                          # กลับไป 7.0 แล้วเทียบซ้ำเมื่อมีข้อมูลมากพอ

# ---------------------------------------------------------------------------
# Scoring Weights  (รวม = 9)
# ---------------------------------------------------------------------------
# Confirmation (weight 1) ถูกตัดออก 2026-07-26 — backtest 403 วันพบว่าตกถึง 79-91%
# (BTC ผ่าน 6/69 = 8.7%, XAU 22/107 = 20.6%) เพราะเป็น breakout confirmation ที่ต้องรอ
# ราคาทะลุ swing ล่าสุด แต่ swing ต้องรอ right=4 แท่งยืนยัน กว่าจะยืนยันราคามักย่อกลับแล้ว
# (ตอนตกห่างจากจุดที่ต้องทะลุ median -4.7% BTC / -3.2% XAU = ไม่ใช่ "เกือบผ่าน")
# เทียบ 3 แบบ: คงไว้ min8 / ตัด min8 / ตัด min7 -> ตัด+min7 ให้ไม้เยอะสุดและ win rate ดีสุด
# ทั้งคู่ (BTC 7->8 ไม้ 42.9%->50.0%, XAU 9->11 ไม้ 66.7%->72.7%)
# หมายเหตุ: sample เล็กมาก (6-11 ไม้) ยังไม่มีนัยสำคัญทางสถิติ และการทดสอบนี้เปลี่ยน 2 ตัวแปร
# พร้อมกัน (ตัด Conf + ลด MIN_SCORE) ยังไม่ได้แยกว่าตัวไหนคือตัวขับผลจริง — ควรวัดซ้ำเมื่อมี
# ข้อมูลเทรดจริงมากพอ  ฟังก์ชัน swing.check_confirmation ยังอยู่ ใช้แสดงผลอ้างอิงเท่านั้น
#
# OBV 4H/1H: ลองตัดออกแล้ว (2026-07-27) เพราะคำนวณจาก tick_volume ไม่ใช่ real volume
# (OBV 1H ไม่เคยผ่าน merge_real_volume() เลยแม้แต่ BTC — df_1h ใน scoring.py ไม่ถูกเรียก
# merge ต่างจาก df_1d/df_4h; OBV 4H ของ XAU ก็เป็น tick_volume เสมอเพราะไม่มีแหล่ง real
# volume ให้) แต่ backtest เทียบแล้ว (เต็ม 8, min 6) win rate แย่กว่าการคง scorecard เดิม
# ไว้แล้วปรับ MIN_SCORE ตรงๆ ชัดเจน (BTC 37.5% vs 50.0%, XAU 66.7% vs 77.8%) จึง **เอากลับ
# เข้ามาแล้ว** — ข้อจำกัดเรื่อง tick_volume ยังเป็นจริงอยู่ (ไม่ได้แก้) แค่ข้อมูลบอกว่าการ
# ตัดออกไม่ได้ช่วยอะไรที่ MIN_SCORE ทำไม่ได้อยู่แล้ว ควร reconsider อีกทีเมื่อมีข้อมูลเทรด
# จริงมากพอจะวัด win rate ได้แม่นกว่านี้
#
# OBV 1H -> DI 1H (2026-07-27): สลับเกณฑ์ในช่อง 1H น้ำหนักเท่าเดิม (1.0) TOTAL_WEIGHT/MIN_SCORE
# ไม่เปลี่ยน เหตุผลจากการวัด redundancy บน 3000 จุด (ส่วนเกินจาก baseline อิสระ):
#   ทำไมตัด OBV 1H (ไม่ใช่ OBV 4H): ซ้ำกับ OBV 1D ที่เก็บไว้มากกว่า (+9.8 vs 4H ที่ +5.6 บน BTC),
#     lookback ทับกันเกือบหมดกับ 4H (50ชม. vs 40ชม.) และ real volume ของ 1H merge ไม่เสถียร
#     (4H ได้ 200/200 แท่ง แต่ 1H ได้ 90/100 และพังทั้งหมดตอนดึงข้อมูลย้อนหลัง)
#   ทำไม DI 1H (ไม่ใช่ DI 1D/4H): DI 1D ซ้ำกับ bias gate +28~33 และผ่าน 79-86% = เกือบแต้มฟรี
#     (ซ้ำรอย VSA/R:R ที่เพิ่งแก้ไป), DI 4H ซ้ำกับ Trend 4H +40 สูงสุดในตาราง — DI 1H อิสระจาก
#     gate ที่สุด (+4.2) และผ่าน ~53-55% คือกรองได้จริง
#   โบนัส: DI ใช้แค่ High/Low/Close ไม่พึ่ง volume -> ช่อง 1H เปลี่ยนจากสัญญาณที่ข้อมูลมีปัญหา
#     เป็นสัญญาณที่ข้อมูลสะอาด และ XAU ได้คุณภาพเท่า BTC เป็นครั้งแรกในเกณฑ์นี้
# ข้อจำกัด: DI 1H ยังซ้ำกับ Trend 1H (+32.8) และ MACD 4H (+27.2) ไม่มี TF ไหนสะอาดหมดจด
# period=20 ยังไม่ได้ sweep หาค่าที่เหมาะสม (ดู swing.calc_di)
#
# WEIGHT_VSA ตัดออกจาก Scoring 2026-07-27 — วัด Total R (ไม่ใช่ win rate) แล้วพบว่า VSA
# (ทุกเวอร์ชันที่ลอง: เดิม / continuation-only / continuation+neutral) ให้ Total R เท่ากัน
# เป๊ะทั้ง 3 แบบ = ไม่เคยเป็นตัวชี้ขาดผลเลย และ BTC ดีขึ้น 26% เมื่อตัดออก (+18.25R -> +23.00R,
# ได้ไม้เพิ่ม 13->14) ส่วน XAU แย่ลงเล็กน้อย 8% แต่ได้ไม้เพิ่ม 21% (14->17 ไม้)
# หมายเหตุ: WEIGHT_VSA_CLIMAX ใน reversal.py เป็นค่าคงที่แยกต่างหาก ไม่ได้ import จากที่นี่
# — Reversal path (is_climax_bar) ไม่กระทบจากการตัดนี้เลย
# 2026-08-01: check_vsa/check_vsa_trend ใน vsa.py ถูกลบทิ้งแล้ว (ไม่มีใครเรียกใช้อีก) —
# ตอนนี้ Scoring ไม่แตะ VSA เลย
# 2026-08-19: ช่อง 4H เปลี่ยนจาก Trend 4H (close vs EMA50) -> RSI 4H (RSI(14,RMA) vs 50)
# ตามคำสั่งผู้ใช้ — น้ำหนักเท่าเดิม 1.0 TOTAL_WEIGHT/MIN_SCORE ไม่เปลี่ยน (แทนที่ ไม่ใช่เพิ่ม)
#
# ข้อมูลที่วัดก่อนตัดสินใจ (3000 แท่ง 4H, 6 symbol — BTC/XAU/USDJPY/EUR/GBP/US500):
#   redundancy: RSI>50 กับ Trend 4H เห็นตรงกัน 89-92% (baseline ถ้าอิสระกัน ~50-54%)
#     => สองตัวนี้แทบเป็นสัญญาณเดียวกัน การสลับจึงเปลี่ยนผลจริงแค่ ~10% ของเวลา
#   predictive power (ทายทิศราคา 6 แท่งข้างหน้าถูกกี่ %):
#     Trend 4H เฉลี่ย 48.69%  |  RSI>50 เฉลี่ย 49.39%  (RSI ชนะ 5/6 symbol แต่ BTC แพ้)
#     => RSI ดีกว่าเล็กน้อย แต่ทั้งคู่ต่ำกว่า 50% (แย่กว่าโยนเหรียญ) เมื่อใช้เดี่ยวๆ ส่วนต่าง
#        0.7% เล็กพอที่จะเป็น noise ได้ — ยังไม่ได้ backtest เต็มรูปแบบ (TotalR/win rate ผ่าน
#        scorecard จริง) ว่าการสลับนี้ทำให้ผลเทรดดีขึ้นจริงไหม เปลี่ยนตามการตัดสินใจของผู้ใช้
#        ควรเฝ้าดูผลเทรดจริงและพร้อมสลับกลับถ้าแย่ลง
# 2026-08-27: **ตัด Trend 1D ออกจากสกอร์การ์ด** (เดิม 2.0 = น้ำหนักหนักสุดในระบบ)
# เหตุผลไม่ใช่สถิติ แต่เป็นข้อเท็จจริงเชิงโครงสร้าง: จาก backtest_replay.py ที่เดินตามด่านจริง
# ครบทุกข้อ (regime -> bias -> สกอร์การ์ด) ไม้ Scoring ที่ระบบเข้าจริง **46 ไม้จาก BTCUSDm +
# XAUUSDm ผ่านเกณฑ์นี้ 100% ทุกไม้** — ไม่มีตัวอย่างสักตัวที่มันคัดออก
# สาเหตุ: เกณฑ์นี้ถาม "ราคาอยู่ฝั่งเดียวกับเทรนด์ 1D ไหม" ซึ่ง scheduler กรองด้วย regime
# (ADX ยืนยันเทรนด์) และ compute_score บังคับ direction ให้ตรง trend_flip bias มาก่อนแล้ว
# ไม้ที่มาถึงสกอร์การ์ดจึงผ่านข้อนี้โดยอัตโนมัติเสมอ = แจกแต้มคงที่ 2 แต้มให้ทุกไม้เท่ากัน
# ไม่ต่างจากเคสช่อง R:R ที่เคยเป็นแต้มฟรี (ดู MIN_RR ด้านบน) แต่หนักกว่าเพราะน้ำหนัก 2
#
# MIN_SCORE ลดจาก 7 -> 5 พร้อมกัน (TOTAL_WEIGHT 9 -> 7) เพื่อให้ "จำนวนเกณฑ์ที่ต้องผ่านจริง"
# เท่าเดิมเป๊ะ — ไม่งั้นเท่ากับเปลี่ยนสองอย่างพร้อมกันแล้วแยกไม่ออกว่าอะไรมีผล
#
# ⚠️ ถ้าวันหนึ่งด่าน regime หรือ trend_flip bias ถูกปิด/เปลี่ยน เกณฑ์นี้จะกลับมามีความหมาย
# ทันที — ตอนนั้นค่อยพิจารณาเอากลับเข้ามา
# 2026-08-30: **ตัด OBV 4H ออกจากสกอร์การ์ด** (เดิม 1.0) ตามคำสั่งผู้ใช้
# MIN_SCORE คงที่ 5.0 แต่ TOTAL_WEIGHT ลด 7 -> 6 ผลคือด่านเข้มขึ้นด้วย: จากยอมให้ตกได้ 2 ข้อ
# เหลือยอมให้ตกได้ 1 ข้อ — เป็นการเปลี่ยนสองอย่างพร้อมกันโดยตั้งใจ (ผู้ใช้สั่ง "ตัดออกและ
# ทำให้เข้มขึ้น") ตรงกับ arm `--drop="OBV 4H" --min-score=5` ใน backtest_replay.py
#
# ⚠️ **หลักฐานไม่สนับสนุนการเปลี่ยนนี้ — เป็นการตัดสินใจของผู้ใช้ ไม่ใช่ข้อสรุปจาก backtest**
# A/B เต็มรูปแบบ (730 วัน BTCUSDm+XAUUSDm ไม้ Scoring, canary ผ่านทุกรอบ ไม่มี regime error
# และไม่มี volume fallback) ได้ตาราง 2x2 นี้:
#                        เก็บ OBV        ตัด OBV      ผลของการตัด
#   ยอมตกได้ 2 ข้อ    +0.53R (46 ไม้)  +0.52R (46 ไม้)   -0.01R
#   ยอมตกได้ 1 ข้อ    +0.48R (38 ไม้)  +1.70R (40 ไม้)   +1.22R   <- ค่าที่ตั้งตอนนี้
# ผลของการตัดไม่คงเส้นคงวาข้ามระดับความเข้ม (0 กับ +1.22) = หน้าตาของ noise ไม่ใช่ edge
# ส่วนต่าง +1.17R เทียบ baseline มี bootstrap 95% CI [-12.8, +15.2]R  P(ดีกว่า) = 58%
# และทั้งสอง arm พึ่งไม้ 3 ไม้เดียวกัน (+2.29/+1.74/+1.69 = +5.72R) มากกว่าผลรวมทั้งระบบ
# ตัดไม้ที่ดีที่สุดออกไม้เดียว arm นี้เหลือ -0.59R
# เหตุผลเชิงโครงสร้างที่พอสนับสนุนได้: OBV 4H ของ XAU เป็น tick_volume เสมอ (ไม่มีแหล่ง real
# volume) ตามที่เขียนไว้ด้านบน — แต่ข้อนี้จริงมาตั้งแต่ 2026-07-27 แล้วและตอนนั้นวัดได้ว่าตัด
# ออกแย่กว่า (ดู comment ด้านบน) จึงไม่ใช่หลักฐานใหม่
# 👉 **เฝ้าดูผลเทรดจริงใกล้ชิด พร้อมถอยกลับ** — ถอยกลับ = เอา WEIGHT_OBV_4H = 1.0 กลับเข้า
#    TOTAL_WEIGHT + คืนบรรทัด ("OBV 4H", ...) ใน scoring.criteria + ใส่ชื่อกลับใน
#    scoring.SCORECARD_CRITERIA_NAMES  (MIN_SCORE ไม่ต้องแตะ)
# 👉 ทางปิดเคสให้ขาดคือเพิ่ม n: รัน 3 arm กับ ETHUSDm/XRPUSDm/US500m
WEIGHT_OBV_1D        = 1.0
WEIGHT_RSI_4H        = 1.0   # เดิมคือ WEIGHT_TREND_4H (close vs EMA50) — ดู comment ด้านบน
WEIGHT_TREND_1H      = 1.0
WEIGHT_DI_1H         = 1.0
WEIGHT_MACD          = 1.0   # 2026-08-26: น้ำหนักเท่าเดิม แต่ "นิยาม" ของช่องนี้เปลี่ยนไปแล้ว —
                             # จากเดิมตัดสินด้วย Histogram (โมเมนตัมเร่งไหม) เป็นตำแหน่งเทียบเส้น 0
                             # (MACD+Signal อยู่ฝั่งเดียวกับไม้ไหม) ดูตัวเลข backtest + ข้อจำกัดของ
                             # หลักฐานที่ scoring.macd_ok_for_direction()
WEIGHT_RR            = 1.0

TOTAL_WEIGHT = (
    WEIGHT_OBV_1D +
    WEIGHT_RSI_4H +
    WEIGHT_TREND_1H + WEIGHT_DI_1H +
    WEIGHT_MACD +
    WEIGHT_RR
)

RSI_SCORE_PERIOD = 20   # period ของ RSI ที่ใช้ในสกอร์การ์ด (Wilder RMA — ดู indicators.calc_rsi)
                        # 2026-09-12: 14 -> 20 ตามคำสั่งผู้ใช้ ให้ตรงกับ regime_check.DIV_RSI_PERIOD
                        # และ exit_monitor.RSI_PERIOD ที่เปลี่ยนพร้อมกัน
                        # ⚠️ **ค่านี้ไม่มีผลต่อการตัดสินใจใดๆ ของระบบตอนนี้** ไม่ต้องเสียเวลา
                        # backtest มัน ไล่สายได้ดังนี้:
                        #   scoring.py:385  rsi_4h = calc_rsi(df_4h, RSI_SCORE_PERIOD)
                        #   scoring.py:405  -> เกณฑ์ "RSI 4H Rebound" ในลิสต์ criteria เท่านั้น
                        #                      (rsi_4h ไม่ถูกใช้ที่อื่นเลยในไฟล์)
                        #   scoring.py:471  passed = total >= MIN_SCORE
                        #   MIN_SCORE = 0.0 (ดูบรรทัด 387) -> passed เป็น True เสมอ
                        # ตั้งแต่เลิกใช้สกอร์การ์ดกรองไม้ (commit 4457efc) เกณฑ์ทุกข้อในสกอร์การ์ด
                        # รวมทั้งข้อนี้เหลือสถานะเป็น "ตัวเลขที่บันทึกลง log" ล้วนๆ ด่านที่กรองจริง
                        # ใน compute_score คือ hard block 3 ตัว (MIN_RR / MAX_RR /
                        # MAX_TP_DISTANCE_PCT) ซึ่งไม่แตะ RSI เลย
                        # => เปลี่ยนค่านี้ = เปลี่ยนเลข score ที่โผล่ใน CSV/journal เฉยๆ ไม่มีไม้ไหน
                        # เข้าหรือไม่เข้าเพราะมัน ถ้าวันไหนยก MIN_SCORE กลับขึ้นจาก 0 ค่อยกลับมา
                        # อ่านย่อหน้าถัดไป เพราะตอนนั้นมันจะเริ่มมีผลจริง:
                        #   เกณฑ์นี้ต้องการสองอย่างต่อกัน (ย่อมาแตะโซน MID±BUFFER 45-55 **แล้ว**
                        #   เด้งออกนอกโซนฝั่งเทรนด์) RSI(20) เกาะ 50 แน่นกว่า RSI(14) = "แตะโซน"
                        #   ง่ายขึ้นแต่ "เด้งออก" ยากขึ้น สองผลสวนกัน สุทธิต้องวัด เดาไม่ได้
                        #   และโซน 45-55 เองก็จูนมาบน RSI(14) ยังไม่ได้จูนใหม่ให้เข้ากับ 20
RSI_SCORE_MID    = 50   # เส้นกลาง: Long ต้อง RSI > (MID + BUFFER), Short ต้อง RSI < (MID - BUFFER)
# 2026-08-20: เพิ่ม buffer zone รอบเส้น 50 — กัน noise ตอน RSI แกว่งใกล้เส้นกลางในตลาด sideway
# ไม่งั้นคะแนนช่องนี้จะ flip ไป-มาบ่อยตอนราคาแกว่งรอบ 50 พอดี ทั้งที่ทิศทางจริงยังไม่ชัด
RSI_SCORE_BUFFER = 5    # โซนกลาง 45-55 = ไม่ได้คะแนนทั้งสองฝั่ง (ต้อง backtest หาค่าที่เหมาะสม)

# 2026-08-20: RSI Double Rebound — แทนเกณฑ์ "RSI 4H" เดิม (threshold เดี่ยว ณ แท่งปัจจุบัน) ด้วย
# pattern ต่อ trend: RSI ย่อมาแตะโซนกลาง (RSI_SCORE_MID ± RSI_SCORE_BUFFER) แล้วเด้งกลับออกไป
# ฝั่ง trend อย่างน้อย N ครั้งใน N แท่งล่าสุด
#
# 2026-08-26: sweep lookback x min_touches บน BTCUSDm + ETHUSDm (1500 แท่ง 4H, forward=6,
# backtest_rsi_rebound.py) — ผลไม่คงเส้นคงวาข้าม symbol เลยสักตัว (เช่น (25,1): BTC Long
# win 55.9% แต่ ETH Long win แค่ 48.5% แย่กว่า threshold เดิมด้วยซ้ำ) Short แย่กว่าสุ่มเดาแทบ
# ทุก config ทุก symbol — ไม่มี config ไหนชนะ threshold เดิมสม่ำเสมอ ความเห็น Claude คือควร
# ถอยกลับไปใช้ threshold เดิม แต่ผู้ใช้เลือกตั้ง (25,1) ต่อตามนี้ — **เป็นค่าที่ backtest แล้ว
# ไม่พบ edge ที่ชัดเจน ไม่ใช่ค่าที่แนะนำ** เฝ้าดูผลจริงใกล้ชิด พร้อมพิจารณาถอยกลับถ้าผลไม่ดี
RSI_REBOUND_LOOKBACK_BARS = 25
RSI_REBOUND_MIN_TOUCHES   = 1    # ต้องแตะ+เด้งอย่างน้อยกี่ครั้งถึงนับผ่านเกณฑ์

# ---------------------------------------------------------------------------
# Symbols ที่ trade
# ---------------------------------------------------------------------------
# 2026-08-02: เพิ่ม ETHUSDm/XRPUSDm — TREND_FLIP_K (scoring.py) sweep แล้วก่อนเปิดใช้งาน
# (k=0.40/0.20 ตามลำดับ, FalseFlip 0% ทั้งคู่) real volume (Bitstamp) ยืนยันแล้วว่า merge ได้
# 100% ทั้ง 2 symbol — swing_vol_multiplier/wick_ratio_min ยังใช้ default เดิม (1.9x, ไม่มี
# wick filter) เหมือน BTC ก่อนจะ tune เฉพาะเจาะจง ยังไม่ได้ backtest ปรับแยกให้ ETH/XRP
#
# 2026-08-09: เพิ่ม USDJPYm — TREND_FLIP_K sweep แล้ว (k=0.20, FalseFlip 0%) เหมือนกัน แต่ไม่มี
# real volume ให้ merge เลย (Forex เป็นตลาด OTC ไม่มี exchange กลาง ไม่ใช่แค่ยังไม่ได้ต่อแหล่ง
# ข้อมูล) เลยเปิด wick_ratio_min=0.5 ชดเชยตั้งแต่แรกเหมือน XAU (ดู comment เต็มที่
# swing.swing_wick_ratio_min) แทนที่จะปล่อย default (None) แบบ ETH/XRP ตอนเริ่มเปิดใช้งาน
#
# 🔴 เช็คลิสต์ตอนเพิ่ม symbol ใหม่ (ทุกช่องต้องตั้งค่า ไม่งั้นได้ default ที่อาจไม่เหมาะ):
#   1. scoring.TREND_FLIP_K        — รัน backtest_trend_flip_ksweep.py หา k (ไม่มี = bias None = SKIP ตลอด)
#   2. swing.swing_vol_multiplier  — default 1.9x
#   3. swing.swing_wick_ratio_min  — default 0.5
#   4. regime_check.ADX_BAR_OFFSET_H — default 0 ⚠️ ต้องเทียบกับจอ TradingView เอง ไม่งั้น ADX
#      ที่ระบบเห็นจะไม่ตรงกับที่คุณดู และอาจตัดสิน regime คนละแบบ (ดูคำอธิบายเต็มในไฟล์นั้น)
#   5. COOLDOWN_HOURS_BY_SYMBOL ด้านล่าง
#
# 2026-08-12: เพิ่ม US500m (S&P500 index, Exness suffix m) — ครบทั้ง 5 ช่องแล้ว: k=0.20,
# vol=2.0x, wick=0.6, ADX offset=2, cooldown 48 ชม. (ดูรายละเอียด/ที่มาของแต่ละค่าในไฟล์นั้นๆ)
# ไม่มี real volume ให้ merge เหมือน XAU/USDJPY — index ซื้อขายกระจายหลาย exchange ไม่มีตัวเลข
# volume รวมศูนย์ ต้องใช้ tick_volume โบรกเป็น proxy
#
# 2026-08-17: เพิ่ม EURUSDm/GBPUSDm — k-sweep แล้ว (EUR: k=0.20 FalseFlip 0%/67, GBP: k=0.20
# FalseFlip 1.3%/78 — เหมือนเคส US500m ไม่ได้ 0% แต่ต่ำสุดในกลุ่มที่ทดสอบ) เป็น Forex OTC เหมือน
# USDJPYm ไม่มี real volume ให้ merge เลย แต่ "ไม่" เปิด wick_ratio_min เฉพาะทันทีแบบ USDJPYm —
# ปล่อยตาม default กลาง (1.9x / 0.5) ไปก่อนเพราะยังไม่ได้ดูตาราง swing จริงเทียบกับผู้ใช้เหมือน
# ที่ทำตอน US500m/USDJPYm (ทั้งคู่ปรับ wick ตามคำสั่งผู้ใช้หลังดูข้อมูลจริง ไม่ใช่ default ตรงๆ)
# ADX_BAR_OFFSET_H ยังไม่ตั้ง (default=0) — ยังไม่ได้เทียบกับจอ TradingView (วัด sensitivity ไว้
# แล้ว: EUR แกว่ง 4.22 จุดตาม offset, GBP แกว่ง 2.54 จุด — ใกล้เคียงตัวอื่นๆ ไม่ผิดปกติ)
#
# 2026-09-07: **เอา XRPUSDm ออก** — เหตุผลเป็นเรื่องต้นทุน ไม่ใช่ "แพ้ในอดีต"
# วัด spread จริงจาก tick 14 วัน (copy_ticks_range, 2.33 ล้าน tick) โบรกตั้ง spread คงที่
# (median = p90 ทุก symbol) ต้นทุนต่อไม้คิดเป็นสัดส่วนของระยะเสี่ยงจริง (spread / ระยะ SL):
#   XRPUSDm  0.159R   <- 17 เท่าของตัวที่แย่รองลงมา
#   GBPUSDm  0.009R   ETHUSDm 0.008R   EURUSDm 0.007R   USDJPYm 0.005R
#   BTCUSDm  0.004R   US500m  0.003R   XAUUSDm 0.002R
# spread ของ XRP = 0.719% ของราคา (BTC 0.013% / ETH 0.040% / EUR 0.007%) = 55 เท่าของ BTC
# ผลรวม: 19 ไม้ x 0.159R = -3.02R จากที่ XRP ทำได้จริง -2.43R => กลยุทธ์บน XRP มี edge ดิบ
# ราว +0.6R แต่ spread กินหมดแล้วยังเกิน ไม่ใช่ปัญหาที่แก้ได้ด้วยการจูนพารามิเตอร์
#   8 symbol  200 ไม้ WR 48.5% +17.83R  ->  7 symbol 181 ไม้ WR 49.7% +20.26R
# ⚠️ ผูกกับโบรกนี้เท่านั้น — ถ้าย้ายโบรกหรือ Exness ลด spread XRP ลง ให้วัดใหม่แล้วเอากลับเข้ามาได้
# ETHUSDm **เก็บไว้** ทั้งที่ทำได้ -1.05R: ต้นทุน 0.008R/ไม้ = ปกติ ไม่มีเหตุผลเชิงกลไกรองรับ
# การตัด (ตัดออกได้ +1.05R และ WR 49.7->52.1% แต่นั่นคือ curve fitting ล้วนๆ)
SYMBOLS = ["BTCUSDm", "XAUUSDm", "ETHUSDm", "USDJPYm", "US500m", "EURUSDm", "GBPUSDm"]

# ---------------------------------------------------------------------------
# Cooldown หลังปิดไม้ — กันเข้าไม้ symbol เดิมซ้ำเร็วเกินไปหลังปิด (นับทั้งกำไร/ขาดทุน)
# ---------------------------------------------------------------------------
# 2026-08-07: เข้าไม้ผิดทางแล้วรีบเข้าใหม่ทิศทางเดิมซ้ำมักติดกับดักเดิมซ้ำ — พัก 48 ชม. ทุก symbol
# ให้โครงสร้างเปลี่ยนก่อนค่อยพิจารณาใหม่
#
# 2026-08-13: **ปิด cooldown ชั่วคราว (ตั้งเป็น 0 ทุกตัว) ตามคำสั่งผู้ใช้**
# 0 = ปิด (journal.check_cooldown คืน True ทันทีเมื่อ cooldown_hours <= 0 ไม่แตะ CSV เลย)
# บริบท: เพิ่งพบว่า guard ตัวนี้ไม่เคยทำงานจริงเลยตั้งแต่ตั้งค่ามา (เรียกจาก bot.py ที่เป็น
# dead code เท่านั้น — ดู commit 7b592f5) พอต่อเข้า scheduler.py แล้วมันเริ่มบล็อกจริง ซึ่ง
# เท่ากับเปลี่ยนพฤติกรรมระบบทันทีโดยไม่เคยมีข้อมูลว่า 48 ชม. เหมาะไหม (ตัวเลข 48 มาจากการ
# ตัดสินใจเชิงหลักการเมื่อ 2026-08-07 ไม่ได้มาจาก backtest) — ปิดไว้ก่อนเพื่อเก็บข้อมูลเทรด
# จริงแบบไม่มี cooldown ค่อยตัดสินใจทีหลังว่าจะเปิดกลับที่กี่ชั่วโมง
#
# ⚠️ ผลของการปิด: ระบบเข้าไม้ symbol เดิมซ้ำได้ทันทีหลังปิดไม้ (รอบสแกนถัดไป = ภายใน 1 ชม.)
# เคสจริงที่เคยเกิดตอน guard ยังไม่ทำงาน: XAUUSDm ปิด 00:20 -> เปิดใหม่ 01:30 ห่างแค่ 70 นาที
#
# วิธีเปิดกลับ: เปลี่ยนเลขเป็นจำนวนชั่วโมงที่ต้องการ (ค่าเดิมก่อนปิดคือ 48 ทุกตัว)
COOLDOWN_HOURS_BY_SYMBOL = {
    "BTCUSDm": 0,
    "ETHUSDm": 0,
    "XRPUSDm": 0,
    "XAUUSDm": 0,
    "USDJPYm": 0,
    "US500m": 0,
    "EURUSDm": 0,
    "GBPUSDm": 0,
}

# ---------------------------------------------------------------------------
# MT5 Timeframe mapping
# ---------------------------------------------------------------------------
MT5_TIMEFRAMES = {
    "1D": mt5.TIMEFRAME_D1,
    "4H": mt5.TIMEFRAME_H4,
    "1H": mt5.TIMEFRAME_H1,
}
