# -J-Trade

บอทเทรด MT5 (Exness) — สแกนทุก 1 ชม. เปิดไม้ตาม 2 กลยุทธ์ (Scoring = ตามเทรนด์, Reversal =
กลับตัว) แล้วจัดการไม้ด้วย `exit_monitor` เป็นรอบทุกชั่วโมง

**ของจริงอยู่ที่ `scheduler.py`** (loop หลัก) + `exit_monitor.py` (กฎออก) ที่เหลือคือเครื่องมือวัด

> **ตัวเลขในไฟล์นี้กำกับวันที่ไว้ทุกตัว — ตัวที่ไม่มีวันที่คือกฎ ไม่ใช่การวัด**
> ตัวเลขทุกตัวที่นี่เป็นภาพนิ่ง ณ วันนั้น และจะผิดทันทีที่ค่าคงที่ตัวใดเปลี่ยน
> (ไฟล์นี้เกิดขึ้นเพราะไปเจอว่าเลข "68%" ที่จดไว้ที่อื่นล้าสมัยไป 2 สัปดาห์โดยไม่มีใครรู้ —
> รูปแบบเดิมจะเกิดกับไฟล์นี้ด้วยถ้าไม่กำกับวันที่) **ก่อนอ้างตัวเลขจากที่นี่ ให้วัดใหม่**

---

## รันยังไง

`MetaTrader5` เป็น extension ของ Windows — **python ฝั่ง mac import ไม่ได้** ทุกอย่างที่แตะ MT5
ต้องรันผ่าน wine:

```bash
./run_wine.sh backtest_replay.py BTCUSDm 730
```

`python3 script.py` ตรงๆ ใช้ได้เฉพาะสคริปต์ที่อ่าน CSV อย่างเดียว ไม่ต่อ MT5

เทสต์: `./run_wine.sh test_exit_labels.py` (25 เคส) · `./run_wine.sh test_portfolio_risk.py` (12 เคส)

⚠️ `test_dashboard.py` (317 บรรทัด) **ยังไม่เคยถูกรันในรอบนี้** — มัน `import pytest` ซึ่ง
**อยู่ใน `requirements.txt` แล้ว แต่ยังไม่ได้ติดตั้งใน wine prefix** ไม่ใช่ข้อจำกัดของ
สภาพแวดล้อม เป็นแค่ของที่ยังไม่ได้ลง ติดตั้งแล้วน่าจะรันได้ (ยังไม่ได้ลองเอง)

---

## 🔴 กติกาการวัด — อ่านก่อนเชื่อตัวเลขใดๆ

ความรู้ที่แพงที่สุดของโปรเจกต์นี้ ทุกข้อมาจากการวัดที่ผิดพลาดมาแล้วจริง

### 1. `backtest_replay.py` คือตัวตัดสินขั้นสุดท้าย — แต่ไม่ใช่ตัวเดียวที่ใช้ได้
เครื่องมืออื่นตัดด่านบางอย่างออกเพื่อให้มี sample พอวัด — **ด่าน regime อย่างเดียวกรองออก
73.6-83.3% ของรอบสแกน** (วัด 2026-09-15 ทั้ง 7 symbol · เลข "68%" ที่เคยจดไว้ที่อื่นเป็นยุคก่อน
"TREND แรงจัด" ย้ายเข้า NO_TRADE) ตัวเลขจาก backtest ที่ไม่มีด่านนี้กลับข้างได้ทั้งหมด

ลำดับที่ใช้จริง — อย่ารัน replay ทุกอย่างโดยไม่จำเป็น (1.5 ชม./ค่า):

| สถานการณ์ | ใช้อะไร |
|---|---|
| คัดกรองหลายค่าในแกนเดียว (ไม่เปลี่ยนชุดไม้) | `backtest_exit_rules` แล้วเอา **ตัวชนะ** ไปยืนยันด้วย replay |
| ตัวเลือกที่ **ไม่แตะไม้เลยสักไม้** (no-op) | **ไม่ต้องยืนยัน** — ไม่มีเวลาออกเปลี่ยน จึงไม่มีผลต่อช่องถือไม้ให้เครื่องมือมองพลาด (เคสจริง: `MAX_SL_WIDEN_R` = 1.2 ตัดสินจาก exit_rules ล้วน ไม่ได้รัน replay) |
| อะไรที่เปลี่ยนชุดไม้ (ดูข้อ 4) | replay เต็มเท่านั้น ห้ามลัด |

### 2. ผลต่าง 2-4R = noise
ชุดไม้ทั้งหมดฟังดูเยอะ แต่ตัวเลือกที่ต่างกันจริงมักแตะไม้แค่หลักสิบ (เคสปี 2026: 5-40 ไม้
จาก ~200) **ห้ามอ่าน TotalR เป็นอันดับตรงๆ**

### 3. ก่อนสรุปทุกครั้ง ต้องคำนวณ 3 อย่าง

| | ทำไม |
|---|---|
| **แยก direct / churn** | direct = ΔR บนไม้ที่เข้าตรงกันทั้งสองรอบ · churn = ไม้ใหม่ลบไม้ที่หาย ไม้ใหม่ไม่ได้ถูกคัดด้วยเกณฑ์ที่ดีขึ้น มันแค่บังเอิญมีช่อง |
| **ดู 5 ไม้ใหญ่สุด** | ถ้ามากกว่าผลสุทธิ = กำลังตัดสินใจจากไม้ไม่กี่ไม้ (2026-09-15: ปิด BE ได้ +4.96R แต่ 5 ไม้ใหญ่ +9.10R ตัดออกเหลือ **−1.75R**) |
| **นับ symbol ที่บวก** | เกณฑ์ที่เคยรับจริง = 6/7 · ที่ปฏิเสธ = 4/7 **ด้วย R เท่ากัน** (2026-09 ตอนมี 7 symbol) |

⚠️ **ตอน diff สองรอบ ห้ามใช้ `R_a != R_b` กรองว่าไม้ไหนเปลี่ยน** — float ไม่เท่ากันเป๊ะ
ให้กรองด้วย `exit_time` / `how` ที่เปลี่ยนจริง (เคยได้ "เปลี่ยน 34 ไม้" ทั้งที่จริง 1 ไม้)

### 4. อะไรเปลี่ยน "ชุดไม้" ต้อง replay เต็มเสมอ
เปลี่ยนแค่ทางออก (กฎ exit, keep%) → ชุดไม้เท่าเดิม → ใช้ `backtest_exit_rules` คัดกรองก่อนได้
แต่ทุกอย่างต่อไปนี้ **เปลี่ยนชุดไม้** = `backtest_exit_rules`/`backtest_trade_sim` ใช้ไม่ได้:
- อะไรก็ตามที่ขยับ **SL** (→ เปลี่ยนระยะ 1R → เปลี่ยน R:R → เปลี่ยนว่าไม้ไหนผ่าน `MIN_RR_HARD_BLOCK`)
- กฎที่ **ปิดไม้ 100%** (คืนช่องถือไม้ ไม้ถัดไปเข้ามาแทน)
- อะไรที่ทำให้ไม้ **ถือนานขึ้น** (ครองช่องนานขึ้น ไม้ถัดไปหายไป)

### 5. ใส่ระดับ "ไม่เปลี่ยนอะไร" เป็นตัวเลือกในชุดกวาดเสมอ
identity check ฟรี — ถ้ามันไม่ออกมาเท่า control เป๊ะ แปลว่า refactor เปลี่ยนพฤติกรรม
โดยไม่ตั้งใจ และผลทั้งชุดทิ้งได้เลย
⚠️ ตรวจว่าค่านั้นเป็น no-op **จริง** — `BREAKEVEN_LEVEL_R=0.0` ใช่ แต่ `SWING_RECENCY_TOL_ATR=0`
ไม่ใช่ (เคสราคาเสมอจะสลับเงียบๆ)

### 6. `backtest_exit_rules` **เอนไปทางสูงเกินจริง**
เทียบกับ replay เต็ม 2 ครั้ง: +2.66 vs +1.86 · +3.86 vs +3.12 (ห่าง 0.80 และ 0.74)
👉 ให้ถือว่าตัวเลขจากมันคือ **ขอบบน** แล้วตัดสินใจเผื่อไว้
⚠️ **n=2 — อย่าเอา 0.75 ไปลบตรงๆ เหมือนเป็นค่าคงที่** นั่นคือการสรุปจาก sample เล็ก
ซึ่งเป็นสิ่งที่ข้อ 3 ของไฟล์นี้เตือนห้ามทำ ถ้ามีการเทียบเพิ่มให้มาอัปเดตตรงนี้

### 7. วัดกฎ exit ทีละตัวจะได้ค่าต่ำกว่าจริงเสมอ
`position_rules` ใช้ `min()` — ปิดกฎเดียว กฎที่เหลือรับช่วงตัดแทนทันที
(2026-09-13 BTC: ปิดทีละตัวรวมกัน +0.33R แต่ปิดพร้อมกัน +1.86R) ต้องวัดชุดรวมคู่เสมอ

---

## เครื่องมือ — ตัวไหนตอบอะไรได้

base อยู่ที่ `replay_trades_<sym>.csv` (7 symbol × 730 วัน) — **สร้างใหม่ได้เสมอ อย่าเชื่อตัวเลข
ที่จำมา ให้เปิดไฟล์ดู** (ภาพนิ่ง 2026-09-15: 200 ไม้ +46.12R · เช็คว่าไฟล์ทันโค้ดไหมด้วยการดูว่า
มีแถว `how = HOLD-CAP` หรือยัง)

| เครื่องมือ | ตอบคำถาม | **มองไม่เห็น / ข้อจำกัด** |
|---|---|---|
| **`backtest_replay.py`** ⭐ | ทุกอย่างที่อยู่ใน symbol เดียว — เดินตามด่านเดียวกับ `scheduler.scan_symbol()` เป๊ะ | อีก 6 symbol ถืออะไรอยู่ · **กฎข่าว** (ปิดทิ้งเมื่อมี `as_of`) |
| `backtest_trade_sim.py` | เดินไม้ที่รู้แล้วว่าเข้า ไปข้างหน้า (~4 นาที/symbol) | **ช่องถือไม้** · cooldown/daily-loss ที่ขยับตามเวลาปิด |
| `backtest_exit_rules.py` | กวาดกฎ exit หลายชุดในรอบเดียว (สร้างบน trade_sim) | เหมือน trade_sim + **ประเมินสูงเกิน ~0.75R** |
| `backtest_entry_lead.py` | "เข้าเร็วขึ้น N ชม. ดีขึ้นไหม" (คงทิศ ไม่เพิ่มไม้) | เหมือน trade_sim · ใช้ `force=True` ข้าม hard block |
| `backtest_blocked_value.py` | "ถ้ามีช่องที่สองจะได้กี่ไม้ กี่ R" | 🔴 การต่อคิวกลืนสัญญาณช่วงหลัง — ตอบได้แค่คำถามที่คิวนั้นจำลอง |
| `backtest_portfolio.py` | เพดานความเสี่ยงระดับพอร์ต (ต่อ 7 symbol เป็นเส้นเวลาเดียว) | ⚠️ ΔR ที่ได้คือ **ขอบบนของต้นทุน** ไม่ใช่ต้นทุนจริง — ใช้ตอบ "เพดานแพงไปไหม" เท่านั้น |
| `backtest_mfe.py` | กำไรสูงสุดที่เคยมีแล้วคืนกลับ (MFE/MAE) จากไฟล์ replay | MFE เป็น **เพดาน** ไม่ใช่กำไรที่รออยู่ |
| `backtest_criteria.py` | เกณฑ์รายข้อในสกอร์การ์ด active บ่อยแค่ไหน / ชนะแค่ไหน | **ไม่กรอง `MIN_SCORE`** = คนละชุดไม้กับระบบจริงโดยตั้งใจ |
| `backtest_scorecard.py` | จำลอง `compute_score` ทีละแท่ง 4H ถือได้ 1 ไม้ | เก่ากว่า replay — ใช้ replay แทนถ้าเลือกได้ |
| `backtest_score.py` | เช็ค score ณ **จุดเดียว** ในอดีต (debug) | ไม่ใช่เครื่องมือวัดผล |
| `backtest_reversal.py` | เดิน Reversal path ทั้งเส้น ณ เวลาในอดีต (debug) | ไม่ใช่เครื่องมือวัดผล |
| `backtest_tp_sweep.py` | หา `TP_FIB_RATIO` แยกราย symbol | ยึดหน้าต่างเวลาจาก `datetime.now()` — เลื่อนทุกรอบที่รัน |
| `backtest_trend_flip_ksweep.py` | หา `TREND_FLIP_K` แยกราย symbol | ดึงแท่งล่าสุดเสมอ ไม่มี `as_of` |
| `backtest_rsi_rebound.py` | เทียบ RSI double rebound vs threshold เดี่ยว | วัด signal rate / predictive power ไม่ใช่ R |
| `backtest_exit_compare.py` | Fixed SL/TP vs Chandelier (ของเก่า BTC 1 เดือน) | เก่ามาก · ยึดหน้าต่างจาก `now()` |
| `inspect_swings.py` | ตาราง swing high/low พร้อม vol/wick ratio | คอลัมน์ `clean` = **volume OR wick** (เกณฑ์ของ SL) — `check_divergence` ใช้ volume ล้วน ต้องดูคอลัมน์ `vol` |

---

## แหล่งความจริงเดียว — อย่าคัดลอกค่าไปไว้ที่อื่น

โปรเจกต์นี้เคยเจ็บจากการมีสำเนาค่าเดียวกันหลายที่มาแล้วหลายรอบ

| ค่า | บ้านจริง | ใครอ่าน |
|---|---|---|
| `SYMBOLS` | `config.py` | ทุกที่ — **อย่า glob `replay_trades_*.csv`** (ไฟล์ของ symbol ที่ถอดออกแล้วยังค้างอยู่) |
| `REGIME_NO_TRADE` / `_TREND` / `_REVERSAL` | `config.py` | scheduler · backtest_replay · backtest_criteria |
| `SPREAD_PCT_BY_SYMBOL` | `config.py` | backtest_replay · backtest_trade_sim (**ตรึงไว้ ห้ามกลับไปอ่าน spread สด**) |
| `MAX_HOLD_DAYS` | `exit_monitor.py` | backtest_replay · backtest_trade_sim |
| `BE_DECISION_PREFIX` | `exit_monitor.py` | backtest_portfolio (จับคู่สตริงจาก `replay_cuts_*.csv`) |
| `BAR_OFFSET_H` | `bars.py` | (`regime_check.ADX_BAR_OFFSET_H` เป็นแค่ชื่อเก่าที่ re-export) |

**เพิ่ม symbol ใหม่** → เช็คลิสต์ 6 ข้อที่ `config.py` (ใกล้ `SYMBOLS`)

---

## กับดักที่เจอแล้ว

**"ATR Trailing SL" ไม่ trail** — `calc_atr_trailing_sl` คืน `pinned_base ± atr_latest` โดย
`pinned_base` ตรึงที่ swing ตอนเข้าไม้ ไม่วิ่งตามราคา เพดานของมันอยู่ใต้ entry ราว 1 ATR
= **ขึ้นไปถึง entry ไม่ได้โดยโครงสร้าง** ผลคือ **กฎ BE เป็นกลไกเดียวทั้งระบบที่ดึง SL ให้แคบลง**
และดึงได้ไกลสุดแค่ entry เป๊ะ (2026-09-15, 200 ไม้: แคบสุด +1.00R · เลยจุด BE **0 ไม้** — ข้อนี้
เป็นผลเชิงโครงสร้าง ไม่ใช่ความบังเอิญของชุดข้อมูล)
👉 ระบบไม่มีอะไรล็อกกำไรระหว่างทาง มีแต่ TP กับ SL

**`how` ไม่ได้บอกว่า SL ขยับ** — จะพิสูจน์ว่า SL ขยับไหมต้องเทียบ `sl` กับ `sl0` เป็น R ตรงๆ
(คอลัมน์มีอยู่แล้วในไฟล์ replay) ตัวเลข TotalR ดูสมเหตุสมผลได้ทั้งที่กลไกที่คิดว่ากำลังวัด
ไม่เคยทำงานเลยสักครั้ง

**backtest ไม่เคยจำลองกฎข่าว** — `analyze_position` ปิด news guard ทิ้งเมื่อมี `as_of`
(คืน `news_simulated=False`) = **ทุกตัวเลข backtest ไม่นับกฎ exit 2 ตัวที่ระบบจริงใช้อยู่**:
checklist ข้อ 6 (ข่าวสงบ 6 ชม. แล้วยังไม่กำไร → ออก 100%) และ Position Sizing ข้อ 5
(ใกล้ข่าว High impact USD ภายใน 1 ชม. → เหลือ 50%) ยังไม่เคยวัดว่ายิงบ่อยแค่ไหน

**อย่า hand-roll สูตรที่มีฟังก์ชันอยู่แล้ว** — `has_high_volume` ใช้ **5 แท่งรวมตัวเอง**
ไม่ใช่ 20 แท่ง การคำนวณเองทำให้ได้ตัวเลขผิดไปหลายเท่าแล้วสรุปผิดตามมา

**`MFE − R` มีความหมายเฉพาะไม้ที่ MFE ≥ 1R** — ต่ำกว่านั้นมันคือขาดทุนตามแผน ไม่ใช่รูรั่ว
(2026-09-14 นับรวมทีแรกได้ 110R จากช่อง SL ช่องเดียว ซึ่งไม่ชี้อะไรเลย)

**ก่อนนับ "ไม้ขาดทุนเกิน 1R" ต้องบวก spread กลับ** — ไม้ที่โดน SL ตรงจุดเดิมเป๊ะก็ได้ −1.00x R
อยู่แล้วจากต้นทุน

**ด่านที่ "รอให้เงื่อนไขดีขึ้น" พังทุกตัว** — R:R และ extension ดีขึ้นเองตอนราคาย่อ ด่านจึงผลิต
ได้แต่ไม้ย่อ ไม่ใช่ไม้ที่จับได้เร็ว (ดู `config.MIN_RR_HARD_BLOCK`) และการคลายด่านตรวจจับ
= **แทนที่**ไม้เดิมด้วยไม้ที่แย่กว่า ไม่ใช่เพิ่มไม้

---

## เขียนโค้ดในนี้ยังไง

- comment เป็นภาษาไทย และเขียน **ทำไม** + **ตัวเลขที่ทำให้ตัดสินใจแบบนั้น** ไม่ใช่แค่ what
- สวิตช์ทดลองใหม่ → **default = พฤติกรรมระบบจริงเสมอ** แล้วเปิดผ่านธงของ `backtest_replay`
  พร้อมติด tag ที่ชื่อไฟล์ผล กัน base ถูกทับ
- ผลที่วัดแล้ว **ไม่เอา** ก็ยังต้องจดไว้พร้อมเหตุผล — กันคนถัดไปลองซ้ำ (ดู 🔴 หลายจุดใน `config.py`)
- คำเตือนที่หมดอายุแล้วให้ลบ/แก้ทันที มันทำให้คำเตือนที่เหลือถูกเมิน
