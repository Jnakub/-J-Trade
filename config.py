import MetaTrader5 as mt5

# ---------------------------------------------------------------------------
# Account & Risk Management
# ---------------------------------------------------------------------------
ACCOUNT_BALANCE = 10000   # เงินทุน (ปรับได้)
RISK_PER_TRADE  = 0.02    # 2% ต่อไม้
MAX_DAILY_LOSS  = 0.06    # ขาดทุนสูงสุดต่อวัน 6%
MIN_RR          = 1.5     # เกณฑ์ "R:R ดีจริง" ในสกอร์การ์ด + สูตร fallback TP
MIN_RR_HARD_BLOCK = 1.0   # 2026-07-23: เกณฑ์ขั้นต่ำสุดที่บล็อกได้ไม่ว่าคะแนนรวมเท่าไหร่
                          # (แยกจาก MIN_RR แล้ว — เดิมใช้ค่าเดียวกันทำให้ hard block กับ
                          # คะแนนสกอร์การ์ดซ้ำซ้อนกัน เหมือนที่แก้ไปแล้วใน reversal.py)
MIN_SCORE       = 7.0     # คะแนนขั้นต่ำจาก TOTAL_WEIGHT (=10)
                          # 2026-07-27: กลับมาเป็น 7.0 — เอา OBV 4H/1H กลับเข้า scorecard
                          # (ดูเหตุผลด้านล่าง) หลัง backtest เทียบแล้วพบว่าการตัด OBV ออก
                          # (เต็ม 8, min 6) ไม่ได้ช่วย win rate เลย เทียบกับแค่ปรับ MIN_SCORE
                          # ตรงๆ บน scorecard เดิม (เต็ม 10) ได้ผลดีกว่าชัดเจน (BTC 50.0% /
                          # XAU 77.8% ที่ min 7 เทียบกับ min 6 ของเวอร์ชันตัด OBV ที่ได้แค่
                          # 37.5% / 66.7%) — ตัวขับผลจริงคือระดับความเข้มของ MIN_SCORE ไม่ใช่
                          # จำนวนเกณฑ์ที่มี จึงคืนค่าเดิมทั้งหมด (เต็ม 10, min 7 = 70%)

# ---------------------------------------------------------------------------
# Scoring Weights  (รวม = 10)
# ---------------------------------------------------------------------------
# Confirmation (weight 1) ถูกตัดออก 2026-07-26 — backtest 403 วันพบว่าตกถึง 79-91%
# (BTC ผ่าน 6/69 = 8.7%, XAU 22/107 = 20.6%) เพราะเป็น breakout confirmation ที่ต้องรอ
# ราคาทะลุ swing ล่าสุด แต่ swing ต้องรอ right=4 แท่งยืนยัน กว่าจะยืนยันราคามักย่อกลับแล้ว
# (ตอนตกห่างจากจุดที่ต้องทะลุ median -4.7% BTC / -3.2% XAU = ไม่ใช่ "เกือบผ่าน")
# เทียบ 3 แบบ: คงไว้ min8 / ตัด min8 / ตัด min7 -> ตัด+min7 ให้ไม้เยอะสุดและ win rate ดีสุด
# ทั้งคู่ (BTC 7->8 ไม้ 42.9%->50.0%, XAU 9->11 ไม้ 66.7%->72.7%)
# หมายเหตุ: sample เล็กมาก (6-11 ไม้) ยังไม่มีนัยสำคัญทางสถิติ และการทดสอบนี้เปลี่ยน 2 ตัวแปร
# พร้อมกัน (ตัด Conf + ลด MIN_SCORE) ยังไม่ได้แยกว่าตัวไหนคือตัวขับผลจริง — ควรวัดซ้ำเมื่อมี
# ข้อมูลเทรดจริงมากพอ  ฟังก์ชัน swing.check_confirmation ยังอยู่ ใช้แสดงผลอ้างอิงเท่านั้น
#
# OBV 4H/1H: ลองตัดออกแล้ว (2026-07-27) เพราะคำนวณจาก tick_volume ไม่ใช่ real volume
# (OBV 1H ไม่เคยผ่าน merge_real_volume() เลยแม้แต่ BTC — df_1h ใน scoring.py ไม่ถูกเรียก
# merge ต่างจาก df_1d/df_4h; OBV 4H ของ XAU ก็เป็น tick_volume เสมอเพราะไม่มีแหล่ง real
# volume ให้) แต่ backtest เทียบแล้ว (เต็ม 8, min 6) win rate แย่กว่าการคง scorecard เดิม
# ไว้แล้วปรับ MIN_SCORE ตรงๆ ชัดเจน (BTC 37.5% vs 50.0%, XAU 66.7% vs 77.8%) จึง **เอากลับ
# เข้ามาแล้ว** — ข้อจำกัดเรื่อง tick_volume ยังเป็นจริงอยู่ (ไม่ได้แก้) แค่ข้อมูลบอกว่าการ
# ตัดออกไม่ได้ช่วยอะไรที่ MIN_SCORE ทำไม่ได้อยู่แล้ว ควร reconsider อีกทีเมื่อมีข้อมูลเทรด
# จริงมากพอจะวัด win rate ได้แม่นกว่านี้
WEIGHT_TREND_1D      = 2.0
WEIGHT_OBV_1D        = 1.0
WEIGHT_TREND_4H      = 1.0
WEIGHT_OBV_4H        = 1.0
WEIGHT_TREND_1H      = 1.0
WEIGHT_OBV_1H        = 1.0
WEIGHT_VSA           = 1.0
WEIGHT_MACD          = 1.0
WEIGHT_RR            = 1.0

TOTAL_WEIGHT = (
    WEIGHT_TREND_1D + WEIGHT_OBV_1D +
    WEIGHT_TREND_4H + WEIGHT_OBV_4H +
    WEIGHT_TREND_1H + WEIGHT_OBV_1H +
    WEIGHT_VSA + WEIGHT_MACD +
    WEIGHT_RR
)

# ---------------------------------------------------------------------------
# Symbols ที่ trade
# ---------------------------------------------------------------------------
SYMBOLS = ["BTCUSDm", "XAUUSDm"]

# ---------------------------------------------------------------------------
# MT5 Timeframe mapping
# ---------------------------------------------------------------------------
MT5_TIMEFRAMES = {
    "1D": mt5.TIMEFRAME_D1,
    "4H": mt5.TIMEFRAME_H4,
    "1H": mt5.TIMEFRAME_H1,
}
